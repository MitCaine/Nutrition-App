+++
schema_version = 1
capsule_revision = 5
id = "GH-152-P1"
title = "Restore frozen 0017 converter compatibility with serving-reference ORM growth"
state = "READY"
task_type = "correction"
risk = "medium"
created = "2026-08-21"
updated = "2026-08-21"
source_issue = "https://github.com/MitCaine/Nutrition-App/issues/152"
base_commit = "ec6dbc8710e7c7beab0f58338fc1aef9db290ef4"
branch = "gh-152-p1-phase5c-0017-compat"
controller = "ChatGPT"
executor = "ChatGPT"
reviewer = "ChatGPT"
delegation = "none"
delegation_constraints = []
blocked = false
blocked_reason = ""
blocked_since = ""
dependencies = []
planning_artifacts = [
  "AGENTS.md",
  "engineering/workflow/TASK_CAPSULE.md",
  "engineering/workflow/STATES.md",
  "apps/backend/app/models/food.py",
  "apps/backend/app/models/recipe.py",
  "apps/backend/app/operators/historical_recipe_converter.py",
  "apps/backend/app/migrations/versions/0017_phase5c_subject_lookup_indexes.py",
  "apps/backend/app/migrations/versions/0027_serving_reference_measurement.py",
  "apps/backend/tests/test_phase5c_bridge_planner_postgres.py",
]
owned_paths = [
  "engineering/capsules/active/GH-152-P1.md",
  "apps/backend/app/models/food.py",
]
allowed_paths = [
  "apps/backend/app/operators/historical_recipe_converter.py",
  "apps/backend/tests/test_phase5c_bridge_planner_postgres.py",
]
forbidden_paths = [
  "apps/backend/app/migrations/**",
  "apps/backend/app/models/recipe.py",
  "apps/backend/app/operators/historical_recipe_performance_fixtures.py",
  "apps/backend/tests/test_phase5c_performance_postgres.py",
  "apps/backend/tests/test_resource_membership_migration_postgres.py",
  "apps/mobile/**",
  "packages/**",
  "docs/**",
]
specialized_qualification = [
  "PostgreSQL 16 frozen-0017 Phase 5C converter qualification",
]
+++

# GH-152-P1 — Restore frozen 0017 converter compatibility with serving-reference ORM growth

## Goal

Restore the Phase 5C historical Recipe converter's ability to execute against
its intentionally frozen application schema at migration 0017 after the
current ServingDefinition ORM mapping gained three reference-measurement
columns at migration 0027.

## Outcome

The converter can again load and convert historical Recipe projections at
0017 without querying columns that do not exist at that schema revision.

Current 0027+ serving-reference behavior remains intact.

The existing converter restart, checkpoint, failure, tamper, and qualification
tests reach their intended behavior instead of collapsing into
`subject_database_failure`.

## Non-goals

- Advancing the historical converter beyond migration 0017.
- Changing migration 0027 or any migration history.
- Repairing the separate performance-fixture nutrient-catalog drift.
- Repairing the separate 0019 metadata-comparison drift.
- Changing Recipe conversion semantics.
- Changing historical archive contents.
- Weakening converter verification or failure behavior.
- Broad ORM loading refactors.

## Background

The unrestricted PostgreSQL backend suite exposed a large Phase 5C failure
cluster while GH-142-P2 was being qualified.

A clean detached-worktree reproduction proved the failures pre-exist P2.

The root PostgreSQL exception is SQLSTATE 42703:
`serving_definitions.reference_quantity` does not exist.

A dynamic model/schema compatibility audit at migration 0017 found exactly four
current mapped columns absent from the historical database:

- `ServingDefinition.reference_quantity`
- `ServingDefinition.reference_unit`
- `ServingDefinition.reference_gram_weight`
- `RecipeIngredient.user_id`

`RecipeIngredient.user_id` is already mapped `deferred=True` specifically for
frozen-0017 converter/qualifier compatibility.

The three ServingDefinition reference fields are currently eager.

Migration 0027 introduced those three nullable fields ten application
migrations after the converter target.

## Authority and precedence

1. GitHub issue https://github.com/MitCaine/Nutrition-App/issues/152 defines the repair series.
2. Historical converter contract keeps execution at migration 0017.
3. Existing `RecipeIngredient.user_id` deferral establishes the repository's
   current compatibility precedent.
4. Migration 0027 defines the three later serving-reference columns.
5. Existing Phase 5C converter PostgreSQL tests define required behavior.
6. Current serving-reference application behavior must remain unchanged.
7. `TASK_CAPSULE.md` and `STATES.md` govern execution.

## Dependencies and prerequisites

- Exact base is `ec6dbc8710e7c7beab0f58338fc1aef9db290ef4`.
- GH-142-P2 remains suspended independently.
- Clean-main reproduction of SQLSTATE 42703 is recorded.
- 0017 compatibility matrix is:
  - 4 mapped columns absent;
  - 3 eager ServingDefinition mismatches;
  - 1 already-deferred RecipeIngredient mismatch.
- Current source blobs:
  - Food model: `9e5e48b1cea24781ac34a21b655468770488b3a8`
  - historical converter: `978db156be2370102d995c9c27466984613918f7`
  - converter PostgreSQL test: `4827f60df13871d995293a9cb644f973acc7f943`
  - migration 0027: `c67893642ecf56f5f7dc43b58c65b52b0547f3e3`
- No migration change is required or permitted.

## Owned surface

Primary expected correction:

- this capsule;
- `apps/backend/app/models/food.py`.

The historical converter and its PostgreSQL test are allowed only if the
minimal mapper-level compatibility repair is insufficient or a narrow
regression assertion is required.

## Allowed changes

Execution may:

- make the three post-0017 ServingDefinition reference fields safe for reads
  performed against the frozen 0017 converter schema;
- follow the existing deferred-column compatibility pattern if qualification
  proves it preserves current behavior;
- make a narrow converter-specific compatibility adjustment instead if mapper
  deferral alone is insufficient;
- add a narrowly scoped regression assertion to the existing converter
  PostgreSQL test if required.

## Forbidden changes

Execution must not:

- change any migration;
- change the converter target revision;
- change historical archive semantics;
- change Recipe conversion outputs;
- weaken checkpoint, restart, verification, failure, isolation, or tamper
  behavior;
- change `RecipeIngredient.user_id`;
- change the nutrient catalog;
- change performance-fixture behavior;
- change 0019 metadata filtering;
- skip or deselect failing tests;
- broaden into unrelated ORM cleanup.

## Acceptance criteria

- [ ] AC-1: At frozen schema 0017, no eager current ORM column required by the
  converter is absent from the database; the three ServingDefinition
  reference-measurement fields are handled compatibly.
- [ ] AC-2: The previously failing transition-baseline/restart converter test
  completes conversion successfully instead of returning
  `subject_database_failure`.
- [ ] AC-3: Phase 5C bridge-planner PostgreSQL converter tests reach their
  intended converter/checkpoint/verification assertions rather than failing
  from missing 0027 columns.
- [ ] AC-4: Current 0027+ serving-reference read/write semantics remain green;
  no migration or schema behavior changes.
- [ ] AC-5: Full unrestricted backend pytest is rerun to classify the remaining
  independent historical failures after P1; P1 does not claim P2/P3 failures
  as fixed.
- [ ] AC-6: Project audit, capsule validation, diff check, canonical review,
  and required PostgreSQL qualification pass with no unrelated changes.

## Required verification

### Focused

Before implementation:

- re-prove exact Food/converter/test/migration blobs;
- re-prove the 0017 compatibility matrix: 3 eager ServingDefinition
  mismatches plus 1 deferred RecipeIngredient mismatch;
- reproduce the SQLSTATE 42703 converter failure.

After implementation:

- re-run the dynamic 0017 compatibility matrix;
- prove zero unsafe eager mapped columns used by the converter are absent;
- run the previously failing transition-baseline/restart test;
- run the Phase 5C bridge-planner PostgreSQL module;
- run serving-reference tests exercising current schema behavior;
- prove migration files remain unchanged.

### Baseline

Run:

- unrestricted backend pytest;
- `python3 scripts/project-audit.py pre-commit`;
- `python3 scripts/validate-task-capsules.py --all`;
- `git diff --check`;
- `scripts/run-review.sh --no-package`.

The unrestricted backend run may still expose the separately proven P2/P3
historical compatibility families. Those must be enumerated exactly and must
not be silently accepted as P1 failures.

### Specialized qualification

Required:

- PostgreSQL 16;
- real migration-0017 conversion clone;
- converter transition-baseline/restart path;
- exact post-repair failure inventory.

No SQLite-only substitute is sufficient.

## Return evidence

Return:

- exact base, branch, and lifecycle objects;
- pre/post 0017 ORM/schema compatibility matrix;
- root SQLSTATE before repair;
- exact changed paths and blobs;
- converter PostgreSQL results;
- current serving-reference regression results;
- unrestricted backend post-P1 failure inventory;
- project-audit and canonical-review evidence;
- specialized PostgreSQL qualification;
- proof migrations were untouched;
- deferred P2/P3 issue-slice status.

## Escalation conditions

Stop and return to the controller if:

- origin/main moves before READY qualification;
- the pre-change compatibility matrix differs from 3 eager + 1 deferred;
- fixing the defect requires advancing the converter past 0017;
- fixing the defect requires migration changes;
- current 0027+ serving-reference semantics would change;
- the repair requires changes outside owned/allowed paths;
- converter output semantics change;
- P2/P3 fixture/test drift must be mixed into this slice.

## Decisions and assumptions

- Revision 5 records the audited P1 contract and promotes it to READY.
- The defect is historical-schema/ORM compatibility drift, not a Recipe
  conversion-domain defect.
- Mapper-level deferral is the leading repair because the repository already
  uses that exact technique for `RecipeIngredient.user_id`, but execution must
  prove it is sufficient before acceptance.
- P1 is intentionally separated from the 0003 nutrient-catalog fixture drift
  and 0019 metadata-comparison drift.
- The unrestricted backend suite is diagnostic authority for what remains
  after P1, not permission to absorb unrelated failures.

## State history

| Date | From | To | Actor | Reason/evidence |
| --- | --- | --- | --- | --- |
| 2026-08-21 | — | DRAFT | ChatGPT | Unrestricted backend qualification exposed historical PostgreSQL failures independently of GH-142-P2. |
| 2026-08-21 | DRAFT | GRILLED | ChatGPT | Clean-main DBAPI probe identified SQLSTATE 42703 on a post-0017 ServingDefinition column. |
| 2026-08-21 | GRILLED | SPECIFIED | ChatGPT | Dynamic compatibility audit proved exactly three unsafe eager ServingDefinition mismatches and one already-deferred RecipeIngredient mismatch. |
| 2026-08-21 | SPECIFIED | DECOMPOSED | ChatGPT | Repair series separated into 0017 ORM compatibility, 0003 performance-fixture catalog compatibility, and 0019 metadata-comparison compatibility. |
| 2026-08-21 | DECOMPOSED | READY | ChatGPT | P1 scope, frozen-schema invariant, regression gates, specialized PostgreSQL qualification, and escalation boundaries are complete. |

## Completion record

- **Reviewed commit:**
- **Merged commit:**
- **Review disposition:**
- **Verification summary:**
- **Specialized qualification:** Required — PostgreSQL 16 frozen-0017 Phase 5C converter qualification.
- **Known warnings:** Existing SQLAlchemy cyclic-FK warning in the historical 0019 metadata comparison is outside P1.
- **Deferred work/follow-up IDs:** GH-152-P2 performance-fixture catalog compatibility; GH-152-P3 0019 metadata-comparison compatibility.
- **Retrospective required:** no — unless repair reveals an additional unmodeled post-0017 ORM dependency.
