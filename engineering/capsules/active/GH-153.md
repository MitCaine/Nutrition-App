+++
schema_version = 1
capsule_revision = 5
id = "GH-153"
title = "Validate remaining supported remote API responses"
state = "READY"
task_type = "implementation"
risk = "medium"
created = "2026-08-22"
updated = "2026-08-22"
source_issue = "https://github.com/MitCaine/Nutrition-App/issues/153"
base_commit = "b4953e5ebf2bdfc4047b4b6fa94ef7ee7768312d"
branch = "gh-153-remaining-remote-response-validation"
controller = "ChatGPT"
executor = "ChatGPT"
reviewer = "ChatGPT"
delegation = "none"
delegation_constraints = []
blocked = false
blocked_reason = ""
blocked_since = ""
dependencies = [
  "GH-139",
]
planning_artifacts = [
  "engineering/workflow/TASK_CAPSULE.md",
  "engineering/workflow/STATES.md",
  "engineering/capsules/completed/GH-139.md",
  "apps/mobile/src/shared/api/client.ts",
  "apps/mobile/src/runtime/remote/mapRemoteError.ts",
  "apps/mobile/src/runtime/remote/remoteNutritionRuntime.ts",
  "apps/mobile/src/features/calendar/types.ts",
  "apps/backend/app/schemas/calendar.py",
  "apps/mobile/src/features/foods/api/types.ts",
  "apps/backend/app/schemas/food.py",
  "apps/backend/app/schemas/nutrition.py",
  "apps/mobile/src/features/logging/api/types.ts",
  "apps/mobile/src/features/logging/api/logResponseSchemas.ts",
  "apps/mobile/src/features/recipes/api/types.ts",
  "apps/backend/app/schemas/recipe.py",
  "apps/mobile/src/features/recipes/api/recipeResponseSchemas.ts",
  "apps/mobile/src/features/usda/api/types.ts",
  "apps/backend/app/integrations/usda/schemas.py",
  "apps/mobile/src/features/ocr/api/types.ts",
  "apps/backend/app/ocr/confirmation_schemas.py",
]
owned_paths = [
  "engineering/capsules/active/GH-153.md",

  "apps/mobile/src/features/calendar/api/calendarApi.ts",
  "apps/mobile/src/features/calendar/api/calendarResponseSchemas.ts",
  "apps/mobile/src/features/logging/api/logApi.ts",
  "apps/mobile/src/features/logging/api/logResponseSchemas.ts",

  "apps/mobile/src/features/foods/api/foodApi.ts",
  "apps/mobile/src/features/foods/api/foodResponseSchemas.ts",

  "apps/mobile/src/features/usda/api/usdaApi.ts",
  "apps/mobile/src/features/usda/api/usdaResponseSchemas.ts",
  "apps/mobile/src/features/ocr/api/ocrApi.ts",

  "apps/mobile/src/features/recipes/api/recipeApi.ts",
  "apps/mobile/src/features/recipes/api/recipeResponseSchemas.ts",

  "apps/mobile/__tests__/calendarApi.test.ts",
  "apps/mobile/__tests__/logApiMapping.test.ts",
  "apps/mobile/__tests__/foodApiResponseValidation.test.ts",
  "apps/mobile/__tests__/foodCreateApiIdempotency.test.ts",
  "apps/mobile/__tests__/foodDelete.test.ts",
  "apps/mobile/__tests__/foodResolvedNutrition.test.ts",
  "apps/mobile/__tests__/usdaApi.test.ts",
  "apps/mobile/__tests__/usdaImportFlow.test.ts",
  "apps/mobile/__tests__/ocrApiResponseValidation.test.ts",
  "apps/mobile/__tests__/recipeApiMapping.test.ts",
  "apps/mobile/__tests__/remoteNutritionRuntime.test.ts",
]
allowed_paths = [
  "apps/mobile/__tests__/foodLogHandoff.integration.test.ts",
  "apps/mobile/__tests__/runtimeHookCallbacks.test.ts",
  "apps/mobile/__tests__/savedFoodsContract.test.ts",
  "apps/mobile/__tests__/routeScreenDetailLayout.test.ts",
  "apps/mobile/__tests__/ui_133FoodDetailsTaskFirst.test.ts",
  "apps/mobile/__tests__/e4_06HistoryQuery.test.ts",
  "apps/mobile/__tests__/nutritionConfirmationScreen.test.ts",
  "apps/mobile/__tests__/ui_134RecipeDetailsTaskFirst.test.ts",
]
forbidden_paths = [
  "apps/backend/**",
  "apps/mobile/src/shared/api/client.ts",
  "apps/mobile/src/runtime/remote/mapRemoteError.ts",
  "apps/mobile/src/runtime/remote/remoteNutritionRuntime.ts",
  "apps/mobile/src/runtime/local/**",
  "apps/mobile/package.json",
  "apps/mobile/package-lock.json",
  "packages/**",
  "migrations/**",
  "alembic/**",
  ".github/**",
  "docs/**",
]
specialized_qualification = []
+++

# GH-153 — Validate remaining supported remote API responses

## Goal

Complete deterministic runtime validation for the supported mobile remote API
response surface that remains outside the bounded high-risk validation completed
by GH-139.

## Outcome

Every supported remote response family selected during discovery fails closed at
its capability API boundary when a successful HTTP response violates the
canonical backend response contract.

Valid canonical responses preserve their current application types and behavior.

The generic `apiRequest` transport remains domain-agnostic.

## Non-goals

- No generic response-validation framework inside `apiRequest`.
- No backend contract redesign.
- No API-version change.
- No local SQLite runtime change.
- No synchronization, fallback, dual write, shadow write, or cross-authority
  cache.
- No UI or navigation redesign.
- No speculative validator for an unsupported or unproven response shape.
- No unrelated code simplification.

## Background

The pre-2.0 audit identified the remaining remote response surface as the only
material runtime-contract cleanup before release.

GH-139 validated the highest-risk Daily Log, History, Complete, Daily Summary,
and Recipe publication response families. Its explicit exclusions include
Recent Entries, edit context, Recipe duplication, Recipe nutrition, Foods,
Targets, OCR, USDA, and Calendar.

Current generic transport still returns successful JSON through a compile-time
TypeScript assertion.

Discovery evidence is frozen outside the repository at:

`/Users/mipoo/nutrition-app-task-output/GH-153-discovery-20260822-175607`

Evidence bindings:

- issue snapshot SHA-256: `dcc8edcebc2e9c45e660147722bfcaca449bf6fb7224b5406a6be17d526cdec0`
- candidate API inventory SHA-256: `7b16d3a58190db2c304fe865a8ed4c3f4b90883ece9903f60ef6f7be09371d59`
- API call-line inventory SHA-256: `447d5d2f5ddf972aeb1c5ddd0da6340018043db8d6ea56d99f12216b64d1b198`
- existing response-schema inventory SHA-256: `5125d5dd936179858ad8860512d9b3c3f82d3033083cb81f0cc5f63fc7f9bce2`
- backend response-authority inventory SHA-256: `9084e9bb377153be794dc438c5e7ef6151b559a616713989823534e21027494c`
- GH-139 exclusion evidence SHA-256: `3ec722796d2d59b1f899f39f192649f282a6600ede4d4b439a187a9af085c5b1`
- generic-client contract SHA-256: `7b2102b604820a546db71fa8b01752f2948a65b6ae638752583b0200745d517a`

## Authority and precedence

1. Current backend Pydantic response models and executable route contracts.
2. GitHub issue #153.
3. Completed GH-139 response-validation contract.
4. Existing capability-local Zod validation precedents.
5. Existing remote runtime error mapping and authority isolation.
6. Repository workflow documents.

## Dependencies and prerequisites

- Exact discovery base is `74a7412c3a878a0751a77796155a7539a5d40448`.
- Expected task branch is `gh-153-remaining-remote-response-validation`.
- GH-153 is open.
- GH-139 is MERGED.
- GH-139 capsule blob is `7dccebd71fbe3578158aa26527e03c65510127af`.
- Generic API client blob is `48b6be0d971dd4b82bb8f405b3f9f41d35703029`.
- Exact implementation ownership is frozen by revision 4.
- Work-package execution order is B -> C -> A -> D -> Q.
- Package C depends on Package B's canonical Food parser.
- A and D have no package dependency.
- Decomposition evidence is `/Users/mipoo/nutrition-app-task-output/GH-153-decomposed-20260822-180725`.
- Surface inventory SHA-256 is `5693eb12927ede54ddfe5521cbc0bdd0e6a38acf629e1970f34e7b60b4df3c43`.
- Implementation-plan SHA-256 is `ddf632c1cb560d7c4af6f4e8023962e8f77aa82f8ce1640384dfb3406784aff0`.
- No implementation edit is authorized until a later READY capsule passes
  strict execution preflight.

## Owned surface

Revision 4 freezes implementation ownership to the paths declared in TOML.

Package ownership:

- GH-153-B: Food API, canonical Food response schema, and direct Food API
  response-validation tests.
- GH-153-C: USDA API/schema, OCR API confirmation wrapper, USDA/OCR direct
  tests, and Package-B Food-parser reuse.
- GH-153-A: Calendar API/schema, residual Logging API/schema, and direct
  Calendar/Logging tests.
- GH-153-D: Recipe API/schema and direct Recipe API tests.
- GH-153-Q: `remoteNutritionRuntime.test.ts` only. Production remote runtime
  composition and error mapping are read-only authorities.

Five new files are authorized:

- `apps/mobile/src/features/calendar/api/calendarResponseSchemas.ts`
- `apps/mobile/src/features/foods/api/foodResponseSchemas.ts`
- `apps/mobile/src/features/usda/api/usdaResponseSchemas.ts`
- `apps/mobile/__tests__/foodApiResponseValidation.test.ts`
- `apps/mobile/__tests__/ocrApiResponseValidation.test.ts`

No other new file is authorized.

### Work package execution plan

### GH-153-B — Food core

Implement first.

Own the complete canonical general Food parser and all Food/nutrient/list/recent/
resolved/delete response parsing. Food-returning mutations converge on this
parser.

Focused checkpoint must pass before Package C begins.

### GH-153-C — USDA and OCR confirmation

Implement second, immediately after B.

Reuse the Package-B Food parser for USDA import and OCR confirmation. Validate
USDA search/preview and OCR confirmation wrapper independently. Preserve
backend-emitted USDA `source_metadata`.

### GH-153-A — Calendar and residual Logging

Implement third.

Validate Calendar state/preview and residual Recent Entries/edit context.
Preserve exactly the five proven optional edit-context `current_*` wire fields.
Existing GH-139 Logging parsers remain semantically unchanged.

### GH-153-D — residual Recipe

Implement fourth.

Add general Recipe/list/nutrition validation while preserving the existing
publication-specialized parser and its stricter invariants.

### GH-153-Q — union qualification

Implement no production behavior.

Use `remoteNutritionRuntime.test.ts` to prove actual read/mutation
`invalid_response` semantics and delegation preservation, then run all focused
and baseline qualification.

Package checkpoints produce external evidence but no lifecycle commit. The
qualified cumulative implementation is committed once at IMPLEMENTED.

## Allowed changes

Within the exact TOML `owned_paths`:

- add capability-local strict Zod response schemas;
- route the 28 classified successful JSON operations through those parsers;
- export/reuse the one general Food parser where specified;
- expand existing response-schema files without weakening existing GH-139
  contracts;
- update direct API tests with canonical backend-complete fixtures;
- add malformed-response matrices;
- add composed-runtime error-classification assertions.

Within TOML `allowed_paths`, existing mocked successful-response fixtures may be
repaired only when strict canonical validation makes an already-supported test
fixture incomplete. Such repairs may add required response fields or canonical
UUID/date/timestamp values but may not change product expectations.

These permissions are executable only from the committed revision-5 READY
checkpoint after strict execution preflight passes.

## Forbidden changes

The TOML forbidden surface is absolute for execution.

In particular:

- no backend/schema/router modification;
- no generic `apiRequest` modification;
- no `mapRemoteError` modification;
- no production `remoteNutritionRuntime` modification;
- no local-runtime modification;
- no migration or persistence change;
- no package/dependency change;
- no shared-contract or transfer change;
- no CI change;
- no UI/navigation change;
- no documentation change outside this capsule;
- no weakening of GH-139, OCR-parse, Recipe-publication, or Target validators;
- no parser permissiveness added solely to keep an incomplete mocked response
  fixture passing.

If an existing baseline test cannot be repaired as a canonical fixture-only
change within the frozen allowed surface, execution stops for controller review.

## Acceptance criteria

- [ ] AC-1: The exact 44-operation inventory remains complete: 28 newly
  validation-required operations, 14 already fully validated operations, and
  2 intentional HTTP 204 no-response-body operations.
- [ ] AC-2: All four Calendar responses and the two residual Logging responses
  are strictly runtime-validated before feature code uses them, including the
  proven five-field omitted-null compatibility form for Daily Log edit
  context.
- [ ] AC-3: The Food capability has one complete canonical general Food parser
  covering full Food, serving-definition, nutrient, list, recent, resolved
  nutrition, nutrient-catalog, delete-result, and Food-returning mutation
  response families; the existing shallow source-only check is no longer the
  response-integrity boundary.
- [ ] AC-4: USDA search and preview are completely runtime-validated; USDA
  import reuses the canonical Food parser; backend-emitted USDA
  `source_metadata` is accepted and preserved rather than rejected as an
  unknown field.
- [ ] AC-5: OCR confirmation strictly validates its wrapper and UUID trace
  identity and reuses the canonical Food parser; existing OCR parse validation
  remains behaviorally unchanged.
- [ ] AC-6: Residual Recipe list/get/create/update/duplicate/nutrition responses
  use strict general Recipe schemas supporting nullable publication identity
  and ordinary `needs_republish` state; the existing narrower GH-139
  publication parser remains publication-specific and unchanged in behavior.
- [ ] AC-7: Newly validated exact-decimal fields accept only the existing
  bounded unsigned plain-decimal lexical contract and preserve the exact
  string; JSON numbers, exponent/sign notation, non-finite text, and arbitrary
  malformed text fail closed.
- [ ] AC-8: Required UUID, canonical date, offset timestamp, boolean,
  nullability, finite enum/unit, integer/count, nested-object, array-item,
  required-field, and unexpected-field contracts fail closed. Backend-emitted
  fields are not made optional merely because current TypeScript presentation
  types mark them optional.
- [ ] AC-9: Malformed successful reads cross the actual remote runtime boundary
  as non-retryable `RuntimeError(kind=invalid_response,
  mutationOutcome=not_applicable)`.
- [ ] AC-10: Malformed successful mutations cross the actual remote runtime
  boundary as non-retryable `RuntimeError(kind=invalid_response,
  mutationOutcome=unresolved)`; ordinary HTTP error and confirmed-non-commit
  classification remains unchanged.
- [ ] AC-11: `deleteLog` and `deleteRecipe` remain intentional HTTP 204
  no-response-body operations, while the 14 already-qualified GH-139/OCR
  parse/Recipe publication/Targets operations remain behaviorally unchanged.
- [ ] AC-12: Generic `apiRequest`, `mapRemoteError` semantics, local SQLite
  authority, remote authority selection, backend behavior, persistence,
  migrations, transfer, synchronization policy, dependencies, API version, UI,
  and navigation remain unchanged unless an implementation contradiction
  triggers controller escalation.

## Required verification

### Focused

Each work package must prove representative valid canonical responses and
malformed successful responses at both its direct feature API seam and, where
applicable, the actual `remoteNutritionRuntime` boundary.

Required malformed classes across the applicable response families include:

- missing and wrong top-level values;
- unknown top-level and nested properties;
- malformed nested object and array items;
- invalid UUID;
- invalid canonical date or timestamp;
- JSON number where an exact decimal string is required;
- signed/exponent/non-finite/arbitrary decimal text;
- invalid enum, unit, boolean, integer, or nullable representation;
- invalid nested Food/Recipe/nutrient/serving objects;
- missing backend-emitted defaulted fields;
- edit-context omission outside the five explicitly supported `current_*`
  compatibility properties.

Package-specific proof:

- A: Calendar API tests, residual Logging API tests, and remote-runtime
  read/mutation classification.
- B: complete Food/nutrient/list/recent/resolved/delete/mutation response
  tests and canonical Food parser reuse proof.
- C: USDA API tests, OCR confirmation tests, canonical Food reuse proof, and
  remote-runtime read/mutation classification.
- D: general Recipe CRUD/list/nutrition tests, existing publication regression,
  and remote-runtime read/mutation classification.

### Baseline

Under the repository-qualified Node runtime:

- TypeScript typecheck;
- Expo configuration validation;
- complete ordinary mobile Jest baseline.

Under repository Python:

- task-capsule validation;
- documentation/repository validation required by the canonical review path;
- project audit;
- `git diff --check`.

### Specialized qualification

Not applicable — GH-153 changes remote mobile response validation only. It does
not alter native modules, permissions, physical-device UI, backend schema,
PostgreSQL, SQLite, migrations, transfer, or persistence authority.

## Return evidence

Return exact:

- base, branch, issue state, and capsule revision/state;
- complete candidate API inventory;
- already-validated versus remaining response families;
- backend response-model authority for each in-scope family;
- exact owned/allowed/forbidden implementation paths once frozen;
- malformed-response and error-classification evidence;
- focused and baseline qualification results;
- warnings, deviations, and explicitly deferred unsupported surfaces.

## Escalation conditions

Stop if discovery shows:

- a backend/mobile contract contradiction requiring product redesign;
- a required backend or migration change;
- an unproven legacy response form that would need guessing;
- a global validation framework is required;
- generic transport must acquire domain knowledge;
- mutation-outcome semantics would change;
- local/remote authority isolation would change;
- another active capsule owns a required path.

## Decisions and assumptions

- Revision 5 qualifies READY against implementation base `b4953e5ebf2bdfc4047b4b6fa94ef7ee7768312d`.
  That exact DECOMPOSED commit contains all approved planning authority;
  the READY commit must contain only this capsule above that base.
- Pre-implementation qualification used Homebrew Node `v24.19.0` and
  produced external evidence at `/Users/mipoo/nutrition-app-task-output/GH-153-ready-20260822-181922`.
- Pre-implementation typecheck SHA-256 is `ed5c9e8d168719ed12992cd64004d3b90a079f0f3f327993dadad7777b6783c9`.
- Pre-implementation Expo-config SHA-256 is `475f85d2f53285b21fc9575af82fd47ee9fbd9fd7a311e024c9209c9e970b16f`.
- Pre-implementation focused-test SHA-256 is `e0001cf4134625359aaf715cd3dec3ca84318225c7c9b5bf4144ae506ca91833`.
- The focused baseline covers Calendar, residual Logging, Food creation/delete/
  resolved nutrition, USDA API/import, Recipe API mapping, and composed remote
  runtime delegation before any implementation edit.
- Existing incomplete successful-response fixtures remain baseline evidence,
  not permission to preserve incomplete wire forms after strict validation is
  implemented.
- Execution order remains B -> C -> A -> D -> Q.
- Any required path outside the frozen owned/allowed surface stops execution
  and returns control to the controller.
- Revision 4 decomposes the frozen 28-operation contract into five execution
  checkpoints: B -> C -> A -> D -> Q. The decomposition evidence root is
  `/Users/mipoo/nutrition-app-task-output/GH-153-decomposed-20260822-180725` with surface inventory SHA-256 `5693eb12927ede54ddfe5521cbc0bdd0e6a38acf629e1970f34e7b60b4df3c43` and
  implementation-plan SHA-256 `ddf632c1cb560d7c4af6f4e8023962e8f77aa82f8ce1640384dfb3406784aff0`.
- Package B is first because it establishes the one general Food response
  parser required by Package C. Package C must follow B immediately to prevent
  a second Food authority from emerging in USDA/OCR.
- A and D are independent of B/C and remain capability-local.
- Q owns only test evidence at the composed remote-runtime boundary. Production
  `remoteNutritionRuntime` and `mapRemoteError` are read-only.
- Existing incomplete mocked success fixtures may be made canonical. This is
  test maintenance required by stricter validation, not permission to weaken
  production schemas or alter product behavior.
- Five and only five new files are authorized by this revision: three
  capability response-schema files and two direct response-validation test
  files.
- Package checkpoints remain uncommitted cumulative implementation evidence.
  One IMPLEMENTED checkpoint will bind the final qualified source union.
- Revision 3 freezes the observable response-validation contract against
  the exact GRILLED authority. The full frozen specification is external
  evidence `/Users/mipoo/nutrition-app-task-output/GH-153-specified-20260822-180410/response-contract.txt` with SHA-256
  `38365d6fafca90373fadcf33c977bd42a076ce1b1a41aa4da6dc02e83d3673ac`.
- Successful JSON in every newly validated operation is requested as `unknown`
  and parsed before feature code dereferences it. Validators remain
  capability-local; generic `apiRequest` remains transport-only.
- Object schemas are strict. Backend-emitted/defaulted response fields are
  required even where existing TypeScript presentation types currently mark
  them optional. No parser may silently default, coerce, or normalize malformed
  successful response data.
- The GH-139 exact-decimal, UUID, date, timestamp, nutrient-unit, strict-object,
  and malformed-response precedents are retained.
- Calendar state requires backend-emitted `calendar_revision` and `today`.
- Daily Log edit context is the sole proven omitted-field compatibility seam in
  this task: exactly five nullable `current_*` authority fields may be absent
  because the current backend route explicitly removes them when null.
- One complete general Food parser is authoritative for ordinary Food
  responses and is reused by USDA import and OCR confirmation. The specialized
  GH-139 Recipe-publication managed-Food schema remains independently stricter.
- General Recipe validation must not inherit publication-only restrictions:
  `published_food_item_id` may be null and `needs_republish` is an ordinary
  boolean outside publication.
- USDA preview must account for backend-emitted `source_metadata` even though
  the existing mobile presentation type does not expose it.
- All new validator failures use ZodError and therefore retain the established
  remote runtime semantics without creating another error classification
  mechanism: malformed reads are non-retryable invalid-response /
  not-applicable; malformed mutations are non-retryable invalid-response /
  unresolved.
- The existing read/mutation classification in `remoteNutritionRuntime` is
  authoritative, including Calendar preview as a read.
- The two 204 delete operations remain outside response-body validation.
- Revision 2 records the completed GRILLED classification against exact DRAFT
  authority. The frozen classification contains 44 supported remote operations:
  28 require new runtime validation, 14 are already fully validated, and 2 are
  intentional HTTP 204 no-response-body operations.
- The classification evidence root is `/Users/mipoo/nutrition-app-task-output/GH-153-classification-20260822-175852`.
  Bound evidence SHA-256 values are: operation matrix `ce4d76c9147b55f1b567651ab1fbb4b3d96210af20059e271d819dcdd9cbd321`,
  classification summary `33ab8a67bc4aa9e40f9fc31c37e992ec3e1effbee04152ac65c4810c2734c276`, static proof `ad132f693d0bc7dadc8b68d6859db8f494820d3dbd4732e301566f8ac9caf6d4`,
  test-owner inventory `e382fee436dc652c7623d312def16c6617fe57db856dbc73074df75a2cf37e53`, and work-package decomposition
  `c3b53b9825236fc13b4cad2af1b0f8de6018af55edde98f72526ee9a633754b6`.
- The 28 validation-required operations are exactly: Calendar 4, Foods 12,
  residual Logging 2, OCR confirmation 1, residual Recipes 6, and USDA 3.
- The 14 already-qualified operations are exactly: eight GH-139 Logging
  operations, OCR parse, Recipe publication, and all four Target operations.
  These remain read-only regression authority for GH-153.
- `deleteLog` and `deleteRecipe` are intentional HTTP 204 operations and require
  no response-body schema.
- GH-153 remains one GitHub issue and one lifecycle capsule with four bounded
  implementation packages. Child issues are not justified by the current
  authority.
- GH-153-A owns Calendar plus residual Logging contracts: four Calendar
  responses, Recent Entries, and Daily Log edit context.
- Daily Log edit context has an intentional compatibility representation:
  backend routing uses `response_model=None` and omits five nullable current
  authority fields when they are absent. The mobile validator must preserve
  that proven wire form rather than making those omitted fields unconditionally
  required.
- GH-153-B owns the Food core contract family and will replace the current
  shallow Food source-only check with complete canonical Food response
  validation. It also covers nutrient catalog, Food lists/recent/resolved
  nutrition, delete results, and Food-returning mutations.
- GH-153-C owns USDA search/preview/import and OCR confirmation. It depends on
  GH-153-B so USDA import and OCR confirmation reuse one canonical Food parser
  rather than defining competing Food authorities.
- GH-153-D owns residual Recipe list/get/create/update/duplicate and nutrition
  response validation. Existing GH-139 Recipe publication validation remains
  authoritative and unchanged.
- Capability-local schemas remain the selected pattern. No generic global
  response-validation framework will be introduced.
- Existing remote error mapping remains the selected failure boundary.
  Malformed successful reads must become non-retryable invalid-response
  failures; malformed successful mutations retain unresolved outcome semantics
  when commit outcome cannot be proven.
- Backend Pydantic response models remain canonical wire authority. Proven
  compatibility forms are preserved explicitly rather than inferred from
  optional TypeScript properties.
- GH-139 is the direct validation precedent, not a contract to widen
  indiscriminately.
- Runtime validators should remain capability-local.
- Current backend response models are presumed canonical until discovery proves
  a contradiction.
- Compile-time typing alone is insufficient at a supported remote boundary.
- Exact implementation surface is deliberately deferred until discovery
  classifies every remaining response family.

## State history

| Date | From | To | Actor | Reason/evidence |
| --- | --- | --- | --- | --- |
| 2026-08-22 | — | DRAFT | ChatGPT | Pre-2.0 audit identified remaining supported remote response validation as the first bounded technical release track. Exact implementation ownership is deferred until repository-wide API discovery is classified. |
| 2026-08-22 | DRAFT | GRILLED | ChatGPT | Classified all 44 supported remote operations: 28 require validation, 14 are already fully validated, and 2 are intentional HTTP 204 paths. Resolved four-package decomposition, Food-parser reuse, edit-context compatibility, no-body exclusions, and preservation of generic transport and authority boundaries. |
| 2026-08-22 | GRILLED | SPECIFIED | ChatGPT | Froze exact capability-local wire-validation behavior for all 28 remaining operations, including canonical primitive rules, strict backend-emitted fields, edit-context omission compatibility, general Food reuse, USDA source_metadata, general-vs-publication Recipe distinction, read/mutation invalid-response semantics, and the two 204 exclusions. |
| 2026-08-22 | SPECIFIED | DECOMPOSED | ChatGPT | Froze exact production/test ownership and execution order B→C→A→D→Q. B establishes the canonical Food parser consumed by C; A and D remain independent; Q owns composed-runtime qualification only. Authorized canonical fixture repairs and five exact new files while preserving backend, generic transport, error mapping, local runtime, persistence, dependency, CI, and UI boundaries. |
| 2026-08-22 | DECOMPOSED | READY | ChatGPT | Bound implementation base to the exact DECOMPOSED commit, passed Homebrew Node-24 typecheck, Expo-config validation, and the focused pre-execution API/runtime baseline, preserved the clean repository, and prepared the capsule-only READY overlay for strict execution preflight. |

## Completion record

- **Reviewed commit:**
- **Merged commit:**
- **Review disposition:**
- **Verification summary:**
- **Specialized qualification:**
- **Known warnings:**
- **Deferred work/follow-up IDs:**
- **Retrospective required:** no — evaluate after completion.
