+++
schema_version = 1
capsule_revision = 2
id = "GH-142-P4"
title = "Extract reusable USDA test support and close residual audit"
state = "IN_PROGRESS"
task_type = "tooling"
risk = "medium"
created = "2026-08-22"
updated = "2026-08-22"
source_issue = "https://github.com/MitCaine/Nutrition-App/issues/142"
base_commit = "9f83b392da7236c4c7ffa85a727449cb7cc6c7fb"
branch = "gh-142-p4-usda-test-support"
controller = "ChatGPT"
executor = "ChatGPT"
reviewer = "ChatGPT"
delegation = "none"
delegation_constraints = []
blocked = false
blocked_reason = ""
blocked_since = ""
dependencies = ["GH-142-P1", "GH-142-P2R1", "GH-142-P3"]
planning_artifacts = [
  "AGENTS.md",
  "engineering/workflow/TASK_CAPSULE.md",
  "engineering/workflow/STATES.md",
  "engineering/capsules/completed/GH-142-P1.md",
  "engineering/capsules/completed/GH-142-P2R1.md",
  "engineering/capsules/completed/GH-142-P3.md",
  "apps/backend/tests/test_stage3_usda_mapper.py",
  "apps/backend/tests/test_stage3_usda_import.py",
]
owned_paths = [
  "engineering/capsules/active/GH-142-P4.md",
  "apps/backend/tests/support/usda.py",
  "apps/backend/tests/test_stage3_usda_mapper.py",
  "apps/backend/tests/test_stage3_usda_import.py",
  "apps/backend/tests/test_stage3_usda_api.py",
  "apps/backend/tests/test_runtime_transaction_contract.py",
]
allowed_paths = []
forbidden_paths = [
  "apps/backend/app/**",
  "apps/backend/alembic/**",
  "apps/backend/app/control_migrations/**",
  "apps/backend/tests/postgres_test_support.py",
  "apps/backend/tests/support/foods.py",
  "apps/backend/tests/support/recipes.py",
  "apps/backend/tests/support/daily_logs.py",
  "apps/backend/tests/test_phase5c*.py",
  "apps/backend/tests/test_issue_146_complete_runtime_authority_postgres.py",
  "apps/backend/tests/test_e2_15_exporter_postgres.py",
  "apps/backend/tests/test_targets.py",
  "apps/backend/tests/test_target_tracking_preferences.py",
  "apps/backend/tests/test_e4_03_complete_invalidation.py",
  "apps/backend/tests/test_e4_07_daily_summary_complete.py",
  "apps/backend/tests/test_ocr_confirmation.py",
  "apps/backend/tests/test_food_discovery.py",
  "apps/backend/tests/test_recipe_dependency_deletion.py",
  "apps/backend/tests/test_recipe_nested_publication.py",
  "apps/backend/tests/test_recipe_revision_capture.py",
  "apps/backend/tests/test_recipe_revision_publication.py",
  "apps/backend/tests/test_retention_audit.py",
  "apps/mobile/**",
  "packages/**",
  "docs/**",
  "scripts/**",
]
specialized_qualification = []
+++

# GH-142-P4 — Extract reusable USDA test support and close residual audit

## Goal

Complete GH-142 by moving the remaining high-fan-out reusable USDA test
authority out of historical test modules and explicitly classifying every
remaining cross-test import after that extraction.

## Outcome

When P4 is complete:

- `apps/backend/tests/support/usda.py` is the USDA test-support authority for
  `FakeUsdaClient`, `usda_banana_payload`, `usda_branded_bar_payload`, and
  `usda_branded_full_macro_payload`;
- `test_stage3_usda_import.py` no longer defines `FakeUsdaClient`;
- `test_stage3_usda_mapper.py` no longer defines the three moved payload
  builders;
- exactly five USDA cross-test statements are redirected to
  `tests.support.usda`;
- residual cross-test graph falls exactly from 17 to 12;
- all 12 survivors retain their frozen classification and are deliberately
  outside GH-142 extraction scope;
- collection remains 2262;
- affected and full backend qualification remain green;
- no production behavior changes;
- after P4 is merged/default-branch qualified and archived, all GH-142
  acceptance criteria are satisfied and parent issue #142 is eligible for
  human-owner closure.

## Non-goals

- Driving the residual cross-test graph to zero.
- Refactoring intentionally linked Phase 5/PostgreSQL qualification fixtures.
- Extracting Target-domain `configuration_payload`.
- Extracting E4 complete-invalidation continuation helpers.
- Extracting OCR confirmation payload fixtures.
- Moving recipe scenario-local `_projection_state`.
- Moving capture-specific `_published_recipe`.
- Modifying Food, Recipe, or Daily Log support completed in earlier GH-142
  slices.
- Production, migration, runtime, mobile, contract, or persistence changes.

## Background

GH-142 requires high-fan-out reusable Food, Recipe-publication, Daily Log, and
USDA builders to move into domain-specific test support while deliberately
linked Phase 5/qualification fixture authorities remain separate.

P1 completed Food support. P2R1 completed Recipe-publication support. P3
completed Daily Log support and reduced the cross-test graph to 17.

Fresh P4 discovery on exact archived main `9f83b392da7236c4c7ffa85a727449cb7cc6c7fb` proved:

- 145 backend test modules;
- 17 residual cross-test statements;
- exactly five USDA statements;
- exactly four reusable USDA symbols:
  `FakeUsdaClient`, `usda_banana_payload`, `usda_branded_bar_payload`, and
  `usda_branded_full_macro_payload`;
- all four USDA symbols have zero dependency on another top-level source-test
  helper;
- the five USDA statements touch four test modules;
- removing those five yields residual graph target 12;
- the 12 survivors classify exactly as:
  - five Phase/PostgreSQL qualification-authority links;
  - three recipe scenario-local links;
  - two Target-domain links;
  - one E4 continuation link;
  - one OCR-domain link;
- collection remains 2262;
- project audit and 48 focused audit-tooling tests pass.

Discovery evidence is `/Users/mipoo/nutrition-app-task-output/GH-142-P4-discovery-20260822-000945/discovery-summary.txt` with SHA-256 `51e8243b4cb24ae9ae1ff2c90c6f8f8f937b846bcabdad419ddad6c5bcb55c84`.

Final scope-freeze evidence is `/Users/mipoo/nutrition-app-task-output/GH-142-P4-scope-freeze-20260822-001327/scope-freeze.txt` with SHA-256 `f1597e5c963f5659cd7b2265e06a2c7d83edb35e55796ae1e1bd9c879502f3d6`.

## Authority and precedence

1. GitHub issue #142 defines the four required extraction domains and explicitly
   preserves deliberately linked Phase 5/qualification support.
2. Exact P4 base is `9f83b392da7236c4c7ffa85a727449cb7cc6c7fb`.
3. `test_stage3_usda_import.py` blob `d2e67571b569811128c44bf443af0dddff23253d` defines
   `FakeUsdaClient` authority.
4. `test_stage3_usda_mapper.py` blob `60447db66b5e8c0e8eaac4eb76b0408cfd2796a1` defines the three payload
   builder authorities.
5. Frozen AST SHA-256 identities are:
   - `FakeUsdaClient`: `43d082d934dad4ebbc81729175f651689255b89dc6dcf9e4ab1553cf351b1eed`;
   - `usda_banana_payload`: `36781d00a4b3bf4c154d0c9283bd218b20314331103578215280b1ee3a831839`;
   - `usda_branded_bar_payload`: `390fa133242a6f8bb4d62393cf670070bd9cfffcf24a5e1ac837067bc2eafb0c`;
   - `usda_branded_full_macro_payload`: `ee3123e6f81ecc18b6bad41a60b652385d58299ab28366ac59a0a6fa00ae0964`.
6. Completed GH-142 P1/P2R1/P3 support modules are immutable authorities for
   their respective domains.
7. Existing tests define behavior; P4 relocates support authority without
   changing semantics.
8. `TASK_CAPSULE.md` and `STATES.md` govern lifecycle progression.

## Dependencies and prerequisites

- GH-142 is open.
- GH-142-P1 is MERGED.
- GH-142-P2R1 is MERGED.
- GH-142-P3 is MERGED and archived.
- Exact base is `9f83b392da7236c4c7ffa85a727449cb7cc6c7fb`.
- USDA support module is absent.
- Residual graph is exactly 17.
- USDA owns exactly five residual statements.
- Post-P4 graph target is exactly 12.
- All 12 post-P4 survivors have exact frozen classifications.
- Current collection baseline is 2262.
- Current project audit passes with 48 focused audit-tooling tests.
- No production defect or semantic support change is required.

## Owned surface

P4 owns exactly six repository paths:

- this capsule;
- `apps/backend/tests/support/usda.py`;
- `apps/backend/tests/test_stage3_usda_mapper.py`;
- `apps/backend/tests/test_stage3_usda_import.py`;
- `apps/backend/tests/test_stage3_usda_api.py`;
- `apps/backend/tests/test_runtime_transaction_contract.py`.

Implementation surface is exactly five paths: USDA support plus the four
affected test modules.

Any additional required implementation path is an escalation condition.

## Allowed changes

Execution may:

- create `tests/support/usda.py`;
- relocate `FakeUsdaClient` unchanged;
- relocate `usda_banana_payload` unchanged;
- relocate `usda_branded_bar_payload` unchanged;
- relocate `usda_branded_full_macro_payload` unchanged;
- import those support symbols back into historical source tests where required
  by their own test bodies;
- redirect the exact five frozen USDA cross-test statements to
  `tests.support.usda`;
- make narrow import-order/formatting changes required by those redirects;
- create qualification evidence outside the repository;
- after successful merge/archive/default-branch qualification, return evidence
  that parent #142 is closure-ready.

## Forbidden changes

Execution must not:

- modify any USDA payload value, field, nutrient identity, unit, serving,
  assertion, client behavior, or call-count behavior;
- move `usda_extended_nutrient_payload` or other mapper-local test fixtures not
  imported cross-module;
- modify production USDA integration or service behavior;
- alter any of the 12 classified residual statements;
- refactor Phase 5/PostgreSQL fixture authorities;
- refactor Target, E4 continuation, OCR, or recipe scenario-local fixtures;
- modify Food, Recipe, or Daily Log support;
- modify production backend, migrations, control migrations, mobile, contracts,
  docs, or repository scripts;
- create a generic catch-all fixture module;
- weaken qualification gates;
- close GH-142 before P4 is merged, exact-SHA default-branch qualified, and
  archived.

### Final residual classification

The exact expected post-P4 graph is 12 statements.

Five are deliberately linked Phase/PostgreSQL qualification authorities:

1. `test_e2_15_exporter_postgres` → `test_issue_146_complete_runtime_authority_postgres`;
2. `test_e2_15_exporter_postgres` → `test_phase5c4_target_activation_postgres`;
3. `test_issue_146_complete_runtime_authority_postgres` →
   `test_phase5c4_target_activation_postgres`;
4. `test_phase5c4_cutback_control_postgres` →
   `test_phase5c4_control_postgres`;
5. `test_phase5c4_recovery_qualification_control_postgres` →
   `test_phase5c4_control_postgres`.

Three are recipe scenario-local authorities:

1. `_projection_state` from `test_recipe_dependency_deletion`;
2. `_published_recipe` consumed by `test_recipe_revision_publication`;
3. `_published_recipe` consumed by `test_retention_audit`.

Two are Target-domain `configuration_payload` consumers:

1. `test_e4_03_complete_invalidation`;
2. `test_target_tracking_preferences`.

One is an E4 continuation fixture:

- `test_e4_07_daily_summary_complete` consumes `SOURCE_DATE`, `_create_log`,
  `_patch_log`, and `_set_complete` from `test_e4_03_complete_invalidation`.

One is an OCR-domain fixture:

- `test_food_discovery` consumes `confirmation_payload` from
  `test_ocr_confirmation`.

These are classified residual coupling, not unfinished GH-142 extraction work.

## Acceptance criteria

- [ ] AC-1: `tests/support/usda.py` defines exactly the four approved reusable
  USDA symbols and imports no `test_*.py` module.
- [ ] AC-2: Historical USDA source tests no longer define those four moved
  symbols; only intended local aliases/imports remain.
- [ ] AC-3: All five frozen USDA cross-test statements are redirected to
  `tests.support.usda`; moved USDA imports from historical test modules are
  zero.
- [ ] AC-4: Four-of-four USDA helper/class ASTs preserve frozen semantics and
  all four affected test modules retain identical executable semantics outside
  imports and relocated definitions.
- [ ] AC-5: Residual graph is exactly 12, with exact classification
  5 Phase/qualification + 3 recipe-local + 2 Target + 1 E4 continuation +
  1 OCR; collection remains 2262 and affected suite passes.
- [ ] AC-6: Full backend, project mechanical gates, canonical review, and
  repository-required default-branch PostgreSQL qualification pass with no
  production change; after P4 archive, GH-142 is closure-ready.

## Required verification

### Focused

Before implementation:

- re-prove exact base `9f83b392da7236c4c7ffa85a727449cb7cc6c7fb`;
- re-prove source blobs `60447db66b5e8c0e8eaac4eb76b0408cfd2796a1` and `d2e67571b569811128c44bf443af0dddff23253d`;
- re-prove all four frozen AST identities;
- re-prove USDA support is absent;
- re-prove graph 17;
- re-prove exact five USDA statements;
- re-prove exact 12-survivor classification.

After implementation:

- prove USDA support contains exactly four approved public symbols;
- prove support imports no `test_*.py`;
- prove original source modules no longer define moved symbols;
- prove moved USDA cross-test imports from historical tests = 0;
- prove exactly five statements were removed and graph = 12;
- prove exact 12-survivor classification unchanged;
- prove four-of-four AST semantic identity;
- prove four-of-four affected test modules preserve executable semantics outside
  support imports and relocated definitions;
- prove collection remains 2262;
- run all four affected test modules together;
- prove exact five-path implementation surface.

### Baseline

Run:

- full backend pytest;
- `python3 scripts/project-audit.py pre-commit`;
- `python3 scripts/validate-task-capsules.py --all`;
- `git diff --check`;
- `scripts/run-review.sh --no-package`.

Canonical review may subsume duplicate mechanical checks when its exact
implementation objects and output bundle are bound to the qualification record.

### Specialized qualification

No additional local database suite is required for the USDA support relocation
because P4 changes test-support authority only.

Before P4 is archived and GH-142 closed, the exact default-branch integration
SHA must pass the repository-required `Backend PostgreSQL 16 contracts` check
and all other required default-branch checks.

## Return evidence

Return:

- exact base and branch;
- source blobs and four AST identities;
- discovery/scope evidence hashes;
- lifecycle commit/tree checkpoints;
- USDA support blob;
- exact five implementation paths;
- five redirected USDA statements;
- graph 17 → 12;
- exact final 12-statement classification;
- 4/4 AST equivalence;
- 4/4 affected-module semantic identity;
- collection;
- affected suite;
- full backend;
- mechanical/project audit;
- canonical review bundle;
- exact default-branch check IDs;
- zero-production-change proof;
- archive evidence;
- final parent GH-142 closure disposition.

## Escalation conditions

Stop and return to the controller if:

- `origin/main` moves before READY commit;
- either source blob differs;
- any frozen USDA AST identity differs;
- residual graph differs from 17;
- the five USDA statements differ;
- the 12-survivor classification differs;
- any moved USDA symbol requires another source-test helper;
- `tests/support/usda.py` already exists;
- implementation needs any path beyond the six owned paths;
- any residual Phase/Target/E4/OCR/recipe fixture must change;
- production semantics must change;
- collection changes for P4-attributable reasons;
- post-extraction graph does not equal 12.

## Decisions and assumptions


- Revision 1 freezes P4 as the final GH-142 implementation slice.
- USDA public support authority is exactly four symbols:
  `FakeUsdaClient`, `usda_banana_payload`, `usda_branded_bar_payload`, and
  `usda_branded_full_macro_payload`.
- All four are self-contained with zero source-test helper dependencies.
- P4 removes exactly five cross-test statements and targets graph 12.
- The remaining 12 statements are completely classified and intentionally
  retained.
- Residual cross-test coupling is not itself a GH-142 failure; the issue targets
  high-fan-out reusable authority in four named domains and explicitly preserves
  deliberately linked qualification support.
- Target, E4 continuation, OCR, and scenario-local Recipe cleanup may be future
  maintainability work if their fanout grows, but are not additional GH-142
  slices.
- GH-142 must remain open throughout P4 implementation/review/integration.
- Human-owner closure of GH-142 occurs only after P4 is merged, exact-SHA
  default-branch qualified, and archived.
- Revision 2 starts P4 execution from exact READY checkpoint `01258f72e0dfe37405a5b3e7b8265a1cbdb01557` after strict execution validation passed. Frozen scope report SHA-256 `f1597e5c963f5659cd7b2265e06a2c7d83edb35e55796ae1e1bd9c879502f3d6` remains authoritative. Execution is limited to the six owned paths: one lifecycle capsule plus five implementation paths. USDA support authority is exactly `FakeUsdaClient`, `usda_banana_payload`, `usda_branded_bar_payload`, and `usda_branded_full_macro_payload`. Exactly five frozen USDA cross-test statements may be redirected, with residual graph target 12. The exact 12-survivor classification must remain unchanged: five Phase/PostgreSQL qualification links, three recipe scenario-local links, two Target-domain links, one E4 continuation link, and one OCR-domain link. No production, migration, mobile, shared-contract, Food support, Recipe support, Daily Log support, Phase 5, Target, E4, OCR, or scenario-local Recipe fixture change is authorized.

## State history


| Date | From | To | Actor | Reason/evidence |
| --- | --- | --- | --- | --- |
| 2026-08-22 | — | DRAFT | ChatGPT | P4 opened as final USDA extraction and parent residual-classification slice after P3 archive. |
| 2026-08-22 | DRAFT | GRILLED | ChatGPT | Fresh archived-main audit proved 17 residual statements, five USDA statements, four self-contained USDA symbols, and 12 non-USDA survivors. |
| 2026-08-22 | GRILLED | SPECIFIED | ChatGPT | USDA authority fixed to one domain-specific support module; all 12 survivors classified without scope expansion. |
| 2026-08-22 | SPECIFIED | DECOMPOSED | ChatGPT | Exact six-path lifecycle surface, five-path implementation surface, 17→12 graph target, four AST authorities, and closure classification frozen. |
| 2026-08-22 | DECOMPOSED | READY | ChatGPT | Archived-main baseline, source blobs, discovery evidence, collection 2262, project audit, and exact residual classification provide sufficient execution authority. |
| 2026-08-22 | READY | IN_PROGRESS | ChatGPT | Strict execution validation passed at `01258f72e0dfe37405a5b3e7b8265a1cbdb01557`. Exact six-path lifecycle scope is authorized: four reusable USDA symbols move into one domain-specific support module, exactly five USDA cross-test statements may be redirected, graph target is 17→12, and all 12 classified residual statements remain untouched. |

## Completion record

- **Reviewed commit:**
- **Merged commit:**
- **Review disposition:**
- **Verification summary:** Not yet verified.
- **Specialized qualification:** No P4-specific local database suite required; exact default-branch integration must still pass repository-required PostgreSQL 16 contracts.
- **Known warnings:** Existing `DOMAIN_TOKEN_IN_OPS_MIGRATION` warning for historical control migration `ops_0004_phase5c4_admission.py` is unrelated to P4.
- **Deferred work/follow-up IDs:** None required by GH-142. Classified residuals may be reconsidered only if future fanout makes them shared infrastructure.
- **Retrospective required:** no — unless P4 exposes an unclassified reusable authority.
