+++
schema_version = 1
capsule_revision = 3
id = "GH-165-P3"
title = "Bootstrap trusted task controller"
state = "READY"
task_type = "architecture"
risk = "critical"
created = "2026-08-24"
updated = "2026-08-24"
source_issue = "https://github.com/MitCaine/Nutrition-App/issues/165"
base_commit = "44693b870d1b6d9bc8c5b87d73398225c2fd15c5"
branch = "ops/gh-165-p3-trusted-task-controller"
controller = "ChatGPT GPT-5.6 Sol controller"
executor = "ChatGPT GPT-5.6 Sol bounded executor"
reviewer = "ChatGPT GPT-5.6 Sol independent reviewer"
delegation = "none"
delegation_constraints = []
blocked = false
blocked_reason = ""
blocked_since = ""
dependencies = []
planning_artifacts = [
  "engineering/workflow/TASK_CAPSULE.md",
  "docs/operations/testing.md"
]
owned_paths = [
  ".github/workflows/ci.yml",
  ".github/workflows/trusted-qualification.yml",
  "scripts/task",
  "scripts/task.py",
  "scripts/main-governance.py",
  "scripts/main-qualification.py",
  "scripts/lib/task_authorization.py",
  "scripts/lib/trusted_qualification.py",
  "scripts/lib/qualification_profiles.py",
  "scripts/lib/expo_compatibility.py",
  "scripts/lib/terminal_closeout.py",
  "scripts/README.md",
  "docs/operations/testing.md",
  "engineering/workflow/TASK_CAPSULE.md",
  "engineering/capsules/active/GH-165-P3.md",
  "apps/backend/tests/test_task_controller.py",
  "apps/backend/tests/test_main_governance.py"
]
allowed_paths = [
  ".github/workflows/ci.yml",
  ".github/workflows/trusted-qualification.yml",
  "scripts/task",
  "scripts/task.py",
  "scripts/main-governance.py",
  "scripts/main-qualification.py",
  "scripts/lib/task_authorization.py",
  "scripts/lib/trusted_qualification.py",
  "scripts/lib/qualification_profiles.py",
  "scripts/lib/expo_compatibility.py",
  "scripts/lib/terminal_closeout.py",
  "scripts/README.md",
  "docs/operations/testing.md",
  "engineering/workflow/TASK_CAPSULE.md",
  "engineering/capsules/active/GH-165-P3.md",
  "apps/backend/tests/test_task_controller.py",
  "apps/backend/tests/test_main_governance.py"
]
forbidden_paths = [
  "apps/backend/app/**",
  "apps/backend/alembic/**",
  "apps/backend/app/control_migrations/**",
  "apps/mobile/src/**",
  "apps/mobile/app/**",
  "apps/mobile/modules/**",
  "apps/mobile/plugins/**",
  "apps/mobile/package.json",
  "apps/mobile/package-lock.json",
  "packages/**",
  "docs/historical/**"
]
specialized_qualification = [
  "profile:repository",
  "profile:backend",
  "profile:mobile",
  "profile:postgresql"
]
+++

# GH-165-P3 — Replace candidate-controlled qualification with trusted task controller

## Goal

Build the candidate-independent task authorization, trusted qualification, dedicated-App check
publisher contract, and automated `./scripts/task` operator interface needed to replace Task
Capsules as the security authority. P3 is the bootstrap implementation that must first become
reviewed code on `main`; the first live task using that implementation is deliberately deferred to
GH-165-P4.

## Outcome

The repository contains a reviewed, deterministic replacement controller that is ready to become
the default-branch trust root. `./scripts/task` owns routine mechanics; GitHub Issue authorization
is outside candidate history; the trusted workflow is explicitly dispatched from `main`; candidate
qualification executes without the dedicated-App credential; and a separate finalizer is capable of
publishing an exact-SHA `Main qualification` check through a dedicated App.

All contracts are proven with deterministic fakes and repository tests in P3. No live App secret,
ruleset, or protected-main mutation occurs in P3. After P3 is integrated, GH-165-P4 provisions the
live App, uses this controller for its own disposable pilot, proves GitHub behavior, activates
protection only after successful proof, and supplies the owner-facing workflow evaluation.

## Non-goals

- Do not weaken independent verification, independent review, or explicit human merge authority.
- Do not make pull requests, GitHub approving reviews, merge queue, or linear history mandatory
  merely to replace Task Capsules.
- Do not require candidate repository history to prove its own authorization.
- Do not expose the dedicated GitHub App private key to candidate-executing jobs.
- Do not change product runtime behavior, migrations, dependency versions, or mobile native code.
- Do not delete the existing capsule machinery until the replacement pilot is proven and reviewed.
- Do not activate main protection before the trusted check source is proven on a disposable task.

## Background

GH-165-P2 successfully automated substantial capsule and exact-SHA qualification mechanics, but
independent review proved a systemic trust defect: a candidate lineage could author its own READY
authority, and a qualification ref could replace the GitHub Actions workflow that produced a
same-named `Main qualification` check. Binding the required status to the generic GitHub Actions App
therefore did not identify the reviewed qualification producer.

The replacement keeps the proven reasoning gates but moves security authority outside candidate
history.

## Authority and precedence

- GitHub issue #165 is the governing audit authority.
- This READY capsule is the bounded bootstrap authority for implementing the replacement.
- `engineering/workflow/TASK_CAPSULE.md` governs this migration task until cutover.
- `docs/operations/testing.md` governs retained repository verification expectations.
- After the replacement pilot is accepted, the new task-controller contract supersedes capsule
  authority for future tasks; that cutover must be explicit rather than inferred.

## Dependencies and prerequisites

- Current `origin/main` must remain the exact P3 base until bootstrap integration is explicitly
  authorized.
- GH-165-P2 remains REVIEWED / Changes requested and is retained as evidence of the rejected
  candidate-controlled design.
- P3 does not require a live GitHub App credential. All App-token/check-publication behavior must be
  deterministic and injectable under tests.
- `.github/workflows/trusted-qualification.yml` cannot be treated as a live default-branch trust
  root until this bootstrap has independently passed verification/review and is integrated.
- GH-165-P4 is the required post-bootstrap live pilot/cutover stage and will use the P3 task
  controller rather than another Task Capsule if P3 review approves that bootstrap contract.

## Owned surface

- `./scripts/task` and its Python controller.
- Candidate-independent GitHub Issue authorization parsing and validation.
- Trusted qualification orchestration and dedicated-App check publication.
- `Main qualification` governance/ruleset construction.
- Qualification profile reuse from existing repository mechanics.
- Migration documentation and deterministic controller tests.
- The P3 bootstrap capsule only.

## Allowed changes

- Reuse or adapt proven GH-165-P2 qualification/profile/evidence code when it remains useful but
  remove candidate-history authority assumptions.
- Add a trusted `workflow_dispatch` qualification workflow intended to execute from `main`.
- Add machine-readable task authorization and state/evidence formats backed by GitHub Issue
  comments.
- Keep the legacy capsule CLI available during migration and document it as transitional after the
  pilot proves the replacement.
- Add bounded regression fixtures that simulate forged authorization, candidate-controlled
  workflows, stale base, scope violations, failed suites, and check-source spoofing.

## Forbidden changes

- No product runtime, database migration, dependency/toolchain, OCR, native-module, or package-lock
  changes.
- No ruleset activation during bootstrap implementation.
- No routine bypass of failed qualification.
- No App credential in repository files, artifacts, candidate worktrees, candidate jobs, or logs.
- No automatic acceptance decision, automatic review approval, or automatic human-merge decision.
- No deletion of legacy capsule authority before the disposable replacement pilot is accepted.

## Acceptance criteria

- [ ] AC-1: `./scripts/task` is the single stable replacement entry point, exposes deterministic task-controller commands, and emits compact JSON/checkpoint summaries suitable for human or automated callers.
- [ ] AC-2: The authorization format is external to candidate Git history and canonically binds issue/task identity, exact base SHA, allowed paths, forbidden paths, qualification profiles, revision, nonce, authorization comment ID, trusted author identity, and SHA-256 digest.
- [ ] AC-3: Authorization validation fails closed on wrong author, wrong issue, malformed/duplicate authority, edited content/digest mismatch, stale base, unsupported profile, or ambiguous authorization; deterministic GitHub API fakes cover positive and negative cases.
- [ ] AC-4: Candidate scope is calculated from the externally authorized base to the exact candidate SHA; forbidden or unexpected paths fail closed and candidate files cannot broaden scope or reduce qualification profiles.
- [ ] AC-5: `.github/workflows/trusted-qualification.yml` is structurally a default-branch `workflow_dispatch` controller whose inputs identify an external authorization and exact candidate SHA/ref rather than selecting candidate workflow code as the authority.
- [ ] AC-6: Trusted qualification reuses the existing repository profile suites through trusted default-branch workflow code; candidate code is checked out only at the exact supplied SHA and candidate-executing jobs receive no dedicated-App credential.
- [ ] AC-7: A separate privileged finalizer executes no candidate code, obtains a dedicated GitHub App token only in that trusted job, revalidates authorization/result binding, and constructs `Main qualification` on the exact candidate SHA.
- [ ] AC-8: Dedicated-App check publication is injectable and deterministically tested for exact SHA, check name, conclusion, source/config identity, duplicate/mismatched evidence, failed-suite behavior, and API failure without requiring a live private key.
- [ ] AC-9: Main-governance planning accepts a reviewed dedicated-App integration ID rather than the generic GitHub Actions integration and produces loose required-status semantics, no routine bypass, and force-push/deletion protection without applying a live ruleset in P3.
- [ ] AC-10: `task qualify` mechanics validate external authority locally, publish only the exact candidate ref required for remote checkout, dispatch the trusted workflow explicitly at `main`, bind issue/comment/digest/SHA inputs, wait for the exact workflow/App-check identity, fail closed on drift, and clean the temporary ref; transport is fully fakeable for P3 tests.
- [ ] AC-11: Verification, review, and integration controller contracts preserve explicit reasoning boundaries: automation may gather evidence and enforce prerequisites but cannot infer acceptance, mark review Approved, or authorize the human merge without explicit caller input.
- [ ] AC-12: Documentation defines the bootstrap/cutover boundary, dedicated-App minimum permission/secret isolation, compact future operator flow, legacy-capsule compatibility period, and GH-165-P4 live-pilot procedure; all affected repository tests and baseline review pass before P3 approval.

## Required verification

### Focused

- Deterministic authorization canonicalization, comment parsing, digest, trusted-author, ambiguity,
  stale-base, scope, and profile tests.
- Task-controller state/transport tests for prepare/status/qualify/verify/review/integrate contracts.
- Trusted-workflow structural tests proving dispatch-from-main, exact candidate checkout, secret
  isolation, trusted reusable profile execution, and separate finalizer.
- Dedicated-App check-construction tests with injected/fake transport.
- Main-governance payload tests using a synthetic dedicated-App integration ID.
- Relevant existing capsule/governance/profile tests retained wherever the bootstrap reuses those
  contracts.

### Baseline

- Repository-owned mechanical validation.
- Backend affected baseline.
- Mobile affected baseline.
- `Run Nutrition Review.command` / authoritative review bundle before final review.

### Specialized qualification

- `profile:repository`
- `profile:backend`
- `profile:mobile`
- `profile:postgresql`
- Static/default-branch workflow trust-boundary inspection.
- No live dedicated-App secret, `workflow_dispatch`, ruleset activation, or protected-main test in
  P3. Those are mandatory GH-165-P4 acceptance gates after P3 reaches `main`.

## Return evidence

- Exact P3 READY revision-2, substantive, verification, and review commits.
- Changed files/rationale and deterministic controller-test results.
- Canonical authorization fixture plus expected SHA-256/comment-binding evidence.
- Trusted-workflow structural proof showing candidate jobs and privileged finalizer separation.
- Fake/injected dedicated-App request evidence proving exact-SHA check construction without secret
  material.
- Planned dedicated-App permissions, expected App/integration configuration inputs, and required
  Actions secret names without any private-key bytes.
- Main-governance dry-run payload using a synthetic reviewed App integration ID.
- Authoritative review-bundle identifier and SHA-256.
- A concrete GH-165-P4 handoff whose first operation uses `./scripts/task`, including the expected
  compact operator interaction for the owner to evaluate.

## Escalation conditions

- GitHub cannot bind the required check to the dedicated App integration.
- The App check cannot be created on an arbitrary exact repository SHA.
- Candidate code can access the App credential or execute in the privileged finalizer.
- `workflow_dispatch` cannot maintain a default-branch workflow trust boundary while testing the
  exact candidate.
- External authorization cannot be made unambiguous and fail-closed.
- Main protection semantics differ materially from the reviewed model.
- Replacement requires mandatory PRs/merge queue or another workflow-policy change not explicitly
  reviewed.
- Product/runtime changes become necessary.
- Any proposed shortcut makes candidate code authoritative over its own authorization or final
  required check.

## Decisions and assumptions

- Retain the proven human/reasoning gates while automating deterministic mechanics.
- Use GitHub Issues/comments as candidate-independent task authorization.
- Use a canonical authorization payload plus exact comment ID and SHA-256 rather than repository
  READY history as future execution authority.
- Run authority-sensitive `./scripts/task` commands from trusted `main`, while passing task
  worktrees/candidate SHAs as data.
- Dispatch trusted qualification explicitly against the default branch (`main`); candidate refs
  supply checkout data only.
- Reuse the established repository qualification profiles instead of creating a second test matrix.
- Candidate-executing jobs never receive the dedicated-App private key or installation token.
- A distinct trusted finalizer owns dedicated-App token creation and authoritative check
  publication and does not execute candidate code.
- P3 implements and proves these contracts without live credentials or governance mutation.
- After P3 integration, GH-165-P4 becomes the first live task-controller pilot: provision the App,
  prove exact-SHA check publication/source identity, demonstrate forged/out-of-scope/fake-check
  rejection, activate the ruleset only after those proofs pass, and let the owner evaluate the
  operator flow.
- Task Capsules remain the bootstrap harness through P3 only. Retirement is decided after the P4
  pilot rather than assumed by P3 implementation.

- Revision 2 execution and review-repair evidence remains recoverable at exact IMPLEMENTED checkpoint `8602eb53e7dc11049925915149f7af4305d599f3` and local safety ref `refs/backup/GH-165-P3/revision-2-pre-GH-168`. The substantive repair source is `4f70048d83fa798fe80613ba66078a241c6c39c7`. GH-168 advanced `main` to `44693b870d1b6d9bc8c5b87d73398225c2fd15c5` without overlapping P3-owned paths. That external authority change supersedes revision-2 readiness but does not expand or alter the P3 implementation contract; revision 3 will replay the accepted final implementation onto the new base and require fresh qualification, verification, and review.

## State history

| Date | From | To | Actor | Reason/evidence |
| --- | --- | --- | --- | --- |
| 2026-08-24 | — | DRAFT | ChatGPT GPT-5.6 Sol controller | Replacement architecture created after GH-165-P2 systemic trust-root review. |
| 2026-08-24 | DRAFT | GRILLED | ChatGPT GPT-5.6 Sol controller | Challenged candidate-history, workflow-identity, credential-isolation, bootstrap, and integration trust boundaries. |
| 2026-08-24 | GRILLED | SPECIFIED | ChatGPT GPT-5.6 Sol controller | Selected issue-backed authorization plus dedicated-App check production while preserving exact-SHA and human reasoning gates. |
| 2026-08-24 | SPECIFIED | DECOMPOSED | ChatGPT GPT-5.6 Sol controller | Split bootstrap implementation, App provisioning, trusted-workflow pilot, ruleset activation, and operator-flow evaluation. |
| 2026-08-24 | DECOMPOSED | READY | ChatGPT GPT-5.6 Sol controller | Scope, trust boundaries, escalation conditions, and acceptance are explicit enough for bounded implementation. |
| 2026-08-24 | READY | DECOMPOSED | ChatGPT GPT-5.6 Sol controller | Bootstrap sequencing review found that live workflow_dispatch/App/ruleset proof cannot occur until the trusted controller first reaches default-branch main. Split implementation bootstrap from the live disposable pilot before execution begins. |
| 2026-08-24 | DECOMPOSED | READY | ChatGPT GPT-5.6 Sol controller | Revision 2 removes the bootstrap cycle: P3 now implements and verifies the trusted controller without live credentials/governance mutation; GH-165-P4 is explicitly reserved as the first live task-controller/App/ruleset pilot after P3 reaches main. |
| 2026-08-24 | READY | DECOMPOSED | ChatGPT GPT-5.6 Sol controller | Revision 2 later reached repaired IMPLEMENTED checkpoint 8602eb53e7dc11049925915149f7af4305d599f3, but its fresh qualification was blocked only by external Expo 57 compatibility drift. GH-168 is now terminally integrated and current main is 44693b870d1b6d9bc8c5b87d73398225c2fd15c5. Because base_commit is execution authority, the revision-2 READY checkpoint is superseded. Restore the last READY planning authority, increment to capsule revision 3, adopt the new main base, preserve the complete revision-2 attempt at refs/backup/GH-165-P3/revision-2-pre-GH-168, and require a fresh READY gate before replaying implementation. |
| 2026-08-24 | DECOMPOSED | READY | ChatGPT GPT-5.6 Sol controller | Revision 3 restores execution readiness against terminal main 44693b870d1b6d9bc8c5b87d73398225c2fd15c5 after GH-168. Revision-2 implementation evidence is preserved separately and no P3-owned implementation path overlaps the main advance. Qualification profiles and implementation scope are unchanged; implementation must be replayed only after this new READY authority. |

## Completion record

- **Reviewed commit:**
- **Merged commit:**
- **Review disposition:**
- **Verification summary:**
- **Specialized qualification:**
- **Known warnings:**
- **Deferred work/follow-up IDs:**
- **Retrospective required:** yes — governance/security workflow migration requires a post-pilot operator-flow retrospective.

### Terminal closeout rule

Keep this full capsule under `engineering/capsules/active/` through review. At successful
integration or cancellation, write the unique terminal record to `engineering/capsules/HISTORY.md`
with the final evidence and an exact Git recovery commit/path plus SHA-256 for this full capsule.
Remove this active file in the same closeout change. Do not move or copy it into a per-task
`completed/` archive.
