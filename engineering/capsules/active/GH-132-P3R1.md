+++
schema_version = 1
capsule_revision = 2
id = "GH-132-P3R1"
title = "Strengthen History selected-date affordance"
state = "IN_PROGRESS"
task_type = "implementation"
risk = "medium"
created = "2026-08-22"
updated = "2026-08-22"
source_issue = "https://github.com/MitCaine/Nutrition-App/issues/132"
base_commit = "60f457afef0fffa3c848fca8c15795ee18790dfc"
branch = "gh-132-p3r1-history-selection-affordance"
controller = "ChatGPT"
executor = "ChatGPT"
reviewer = "ChatGPT"
delegation = "none"
delegation_constraints = []
blocked = false
blocked_reason = ""
blocked_since = ""
dependencies = [
  "GH-132-P1",
  "GH-132-P2R1",
  "GH-132-P3",
]
planning_artifacts = [
  "engineering/capsules/completed/GH-132-P1.md",
  "engineering/capsules/completed/GH-132-P2R1.md",
  "engineering/capsules/completed/GH-132-P3.md",
  "engineering/workflow/TASK_CAPSULE.md",
  "engineering/workflow/STATES.md",
  "apps/mobile/src/app/theme/AppTheme.tsx",
  "apps/mobile/src/features/history/screens/HistoryScreen.tsx",
  "apps/mobile/src/features/history/components/HistoryDailyBarChart.tsx",
  "apps/mobile/src/features/history/historyOverview.ts",
  "apps/mobile/__tests__/e4_10HistoryOverview.test.ts",
  "apps/mobile/__tests__/e4_12FocusedNutrientHistory.test.ts",
]
owned_paths = [
  "engineering/capsules/active/GH-132-P3R1.md",
  "apps/mobile/src/features/history/screens/HistoryScreen.tsx",
  "apps/mobile/src/features/history/components/HistoryDailyBarChart.tsx",
  "apps/mobile/__tests__/e4_10HistoryOverview.test.ts",
]
allowed_paths = []
forbidden_paths = [
  "apps/backend/**",
  "apps/mobile/src/app/navigation/**",
  "apps/mobile/src/app/theme/**",
  "apps/mobile/src/features/calendar/**",
  "apps/mobile/src/features/foods/**",
  "apps/mobile/src/features/logging/**",
  "apps/mobile/src/features/ocr/**",
  "apps/mobile/src/features/recipes/**",
  "apps/mobile/src/features/targets/**",
  "apps/mobile/src/features/usda/**",
  "apps/mobile/src/features/history/historyFocusedNutrient.ts",
  "apps/mobile/src/features/history/historyNutritionDetails.ts",
  "apps/mobile/src/features/history/historyOverview.ts",
  "apps/mobile/src/features/history/historyProjection.ts",
  "apps/mobile/src/features/history/historyQuery.ts",
  "apps/mobile/src/features/history/historyRangeModel.ts",
  "apps/mobile/src/features/history/types.ts",
  "apps/mobile/__tests__/e4_12FocusedNutrientHistory.test.ts",
  "apps/mobile/__tests__/appTheme.test.ts",
  "apps/mobile/__tests__/bottomNavigation.test.ts",
  "apps/mobile/package.json",
  "apps/mobile/package-lock.json",
  "packages/**",
  "docs/**",
  "scripts/**",
  ".github/workflows/**",
]
specialized_qualification = [
  "Physically inspect selected numeric and explicit-zero History bars on an iPhone-sized native surface in both light and dark appearance. Selection must be immediately obvious while the original nutrient-series color remains visible in the selected-bar outline.",
  "Physically inspect selected gap and unavailable History dates in both light and dark appearance. A short thick rounded baseline dash must be clearly visible without becoming a large surrounding selected-date rectangle.",
  "Confirm the accepted P3 Calories, Protein, Carbohydrate, and Fat semantic-series colors remain unchanged and focused arbitrary-nutrient History remains generic accent-colored.",
]
+++

# GH-132-P3R1 — Strengthen History selected-date affordance

## Goal

Replace P3's physically undersized circular selected-date marker with a stronger
phone-scale selection treatment while preserving the semantic-series work that
passed native physical qualification.

## Outcome

The accepted P3 overview mapping remains unchanged:

- Calories → `nutritionCaloriesSeries`;
- Protein → `nutritionProteinSeries`;
- Carbohydrate → `nutritionCarbohydrateSeries`;
- Fat → `nutritionFatSeries`.

Selected numeric and explicit-zero bars use two simultaneous cues:

- their fill changes to the existing History selection color supplied from the
  theme, restoring the stronger selected-bar recoloring approved by the human
  owner;
- a 2-pixel outline uses the bar's original `barColor`, keeping its nutrient
  series visibly identifiable.

Selected gap and unavailable dates use a short horizontal baseline dash:

- 16 pixels wide;
- 3 pixels thick;
- rounded line caps;
- centered in the selected date slot near the existing baseline;
- drawn with the same existing selection color.

The 3-pixel-radius circular marker is removed.

Focused arbitrary-nutrient History remains generic `theme.colors.accent` and
uses the same stronger selection treatment.

No palette, nutrition data, History model, target semantics, chart scale,
scrolling, slot geometry, reference-line behavior, selected-day reporting, or
accessibility-selected state changes.

## Non-goals

- No change to any P1 semantic or nutrition-series palette value.
- No change to P3's four-series mapping.
- No whole-screen History identity color.
- No red/green or other nutrition adherence judgment.
- No semantic-class coloring of arbitrary focused nutrients.
- No large rectangle around a selected date slot.
- No chart scale, baseline, viewport, scrolling, slot-width, or touch-target
  redesign.
- No backend, persistence, API, navigation, package, or CI change.
- No hard-coded production color.

## Background

GH-132-P3 mechanically passed all seven acceptance criteria. Native physical
review in both light and dark appearance accepted the Calories, Protein,
Carbohydrate, and Fat semantic-series differentiation, focused arbitrary
nutrient behavior, 7-day and 30-day behavior, and existing chart semantics.

Physical review rejected only the generalized circular selected-date marker.
Its 3-pixel radius was judged too small/subtle at physical-phone scale.

The human owner explicitly confirmed that selected-bar color change was
acceptable. P3R1 therefore restores that stronger visual emphasis while adding
an original-series outline so selected numeric state is not communicated by
fill hue alone. Gap and unavailable slots receive a substantially larger but
still restrained baseline dash.

P3 was cancelled rather than regressed because the correction changes its
post-READY contract. P3R1 deliberately inherits the accepted P3 production
tree from exact cancellation checkpoint `60f457afef0fffa3c848fca8c15795ee18790dfc`.

## Authority and precedence

1. GitHub issue #132 owns the semantic-color objective and requirement that
   color remain supplemental rather than the sole meaning.
2. Human physical review of P3 owns the correction requirement: the semantic
   series passed, the small circle did not, and selected-bar recoloring is
   acceptable.
3. Completed GH-132-P3 revision 4 owns the physical-review disposition and
   accepted predecessor behavior.
4. `HistoryDailyBarChart.tsx` owns chart selection rendering.
5. `HistoryScreen.tsx` owns the theme selection color supplied to the shared
   chart.
6. `e4_10HistoryOverview.test.ts` owns direct History chart/rendering
   regression coverage.
7. `historyOverview.ts`, `AppTheme.tsx`, and
   `e4_12FocusedNutrientHistory.test.ts` remain read-only qualification
   authorities.
8. Workflow documents govern lifecycle progression.

If a lower-precedence source conflicts with a higher-precedence source,
execution stops instead of widening P3R1.

## Dependencies and prerequisites

- GH-132 remains open.
- GH-132-P1 is MERGED.
- GH-132-P2R1 is MERGED.
- GH-132-P3 is CANCELLED after mandatory physical design review.
- Exact P3R1 base is `60f457afef0fffa3c848fca8c15795ee18790dfc`.
- `HistoryScreen.tsx` blob is `8bd78572305422be32e001c95f02671f8a2cea0b`.
- `HistoryDailyBarChart.tsx` blob is `cdcbe9b1592cda8c664dbfc9cfcf018b608f7d91`.
- `e4_10HistoryOverview.test.ts` blob is `27c824403399a82b852ee8c086c46f959f200aaf`.
- Read-only `historyOverview.ts` blob is `923e540ed7ce59a7a31cc963d2644ff7b915ab34`.
- Read-only `AppTheme.tsx` blob is `b46d505aa1d17f29d7a709b1009abc52c10b66f7`.
- Read-only `e4_12FocusedNutrientHistory.test.ts` blob is
  `4ed827a8c508b61f5bdc9d0ae4133c8bd5f8232a`.
- Focused readiness baseline passes; SHA-256 `c7432e9405290faff9742bdea99fb295dfa34ff5b907b03245aa027a8abd1f96`.
- TypeScript readiness baseline passes; SHA-256 `ed5c9e8d168719ed12992cd64004d3b90a079f0f3f327993dadad7777b6783c9`.
- Worktree is clean.

## Owned surface

P3R1 owns exactly:

- this capsule;
- `apps/mobile/src/features/history/screens/HistoryScreen.tsx`;
- `apps/mobile/src/features/history/components/HistoryDailyBarChart.tsx`;
- `apps/mobile/__tests__/e4_10HistoryOverview.test.ts`.

`HistoryScreen.tsx` may change only at the two chart selection-color prop call
sites. Its accepted four-series resolver and all other presentation behavior
are frozen.

## Allowed changes

Execution may:

- rename the chart prop `selectedMarkerColor` to `selectionColor`;
- update only the two corresponding `HistoryScreen` call sites;
- restore selected numeric and explicit-zero fill to `selectionColor`;
- add `stroke={barColor}` semantics to selected numeric/zero bars with an
  exact 2-pixel SVG stroke;
- leave unselected numeric/zero bars exactly on their normal `barColor`;
- remove the selected-date `Circle`;
- render a centered 16-pixel `Line` for selected gap/unavailable dates using
  `selectionColor`, `strokeWidth=3`, and rounded line caps;
- update the direct E4-10 SVG mock and assertions for the new rendering;
- preserve focused arbitrary-nutrient generic-accent behavior.

## Forbidden changes

Execution must not:

- modify `AppTheme.tsx` or any palette value;
- change the four accepted P3 overview series mappings;
- modify History data, query, projection, range, or target authority;
- modify `e4_12FocusedNutrientHistory.test.ts`;
- assign one of the four canonical macro colors to arbitrary focused nutrients;
- introduce a large selected-date surrounding rectangle;
- change chart geometry calculations, scale, baseline, viewport width,
  horizontal scrolling, 44-point slots, touch-target behavior, or
  reference-line math;
- change selected-day textual reporting or accessibility selected state;
- introduce hard-coded colors;
- modify any path outside the exact owned surface.

## Acceptance criteria

- [ ] AC-1: Calories, Protein, Carbohydrate, and Fat retain exactly their
  accepted P3 `nutrition*Series` mappings.
- [ ] AC-2: A selected positive numeric bar uses `selectionColor` as fill and
  an exact 2-pixel `barColor` outline.
- [ ] AC-3: A selected explicit-zero bar uses the same selected fill plus
  original-series outline; unselected numeric and zero bars remain unchanged.
- [ ] AC-4: Selected gap and unavailable dates render a centered 16-pixel,
  3-pixel-thick rounded baseline dash using `selectionColor`.
- [ ] AC-5: No selected-date `Circle` remains and no large selected-slot
  rectangle is introduced.
- [ ] AC-6: Focused arbitrary-nutrient History remains generic accent-colored
  and uses the same numeric/zero outline treatment or empty-slot dash.
- [ ] AC-7: Existing accessibility selected state, selected-day text,
  scale/baseline/reference behavior, 7-day and 30-day layout, horizontal
  scrolling, 44-point date slots, theme, models, data, targets, backend, and
  all other feature behavior remain unchanged.

## Required verification

### Focused

Run:

`cd apps/mobile && npm test -- --runInBand --runTestsByPath __tests__/e4_10HistoryOverview.test.ts __tests__/e4_12FocusedNutrientHistory.test.ts __tests__/appTheme.test.ts __tests__/bottomNavigation.test.ts`

Also prove:

- the exact accepted P3 four-series mapping is unchanged;
- selected numeric/zero fill uses `selectionColor`;
- selected numeric/zero outline uses original `barColor` at exactly 2 pixels;
- selected gap/unavailable state uses one centered 16-pixel rounded SVG line at
  exactly 3 pixels;
- no selected-date circle remains;
- focused arbitrary-nutrient History still uses generic accent;
- `historyOverview.ts`, `AppTheme.tsx`, and E4-12 remain exact base objects;
- no hard-coded production color is added;
- `git diff --check` passes.

### Baseline

Run:

- `cd apps/mobile && npm run typecheck`;
- full mobile Jest baseline;
- `./scripts/project-audit.sh pre-commit`;
- task-capsule validation.

### Specialized qualification

Physically inspect native iPhone-sized History in light and dark appearance:

- selected positive numeric bars are immediately obvious at phone scale;
- selected explicit-zero bars remain visibly selected;
- the selected fill has strong emphasis while the original nutrient-series
  outline remains recognizable;
- selected gap and unavailable dates have a clearly visible restrained
  baseline dash;
- no large surrounding selected-date rectangle appears;
- the accepted Calories/Protein/Carbohydrate/Fat series remain unchanged;
- focused arbitrary-nutrient History remains generic accent;
- 7-day and 30-day selection and scrolling remain usable.

## Return evidence

Return:

- exact base, branch, and changed paths;
- predecessor cancellation commit;
- P3R1 READY and implementation commits;
- qualified source/test blobs;
- selected numeric/zero fill and outline proof;
- empty-slot dash dimensions and rendering proof;
- proof no selected circle remains;
- proof the accepted P3 series mapping is unchanged;
- proof focused arbitrary nutrient remains generic accent;
- focused tests, TypeScript, full mobile, project-audit, and capsule-validation
  outcomes;
- read-only authority rebinds;
- physical light/dark qualification disposition;
- warnings and deviations.

## Escalation conditions

Stop if:

- `origin/main` moves before execution qualification;
- the accepted P3 four-series mapping would need to change;
- the correction requires a theme or palette change;
- the correction requires History model/data/projection changes;
- the selected outline requires chart scale, slot, scrolling, or touch geometry
  changes;
- the baseline dash requires changing date-slot geometry;
- focused arbitrary nutrients would require semantic classification;
- any path outside the exact owned surface is required;
- physical review finds the stronger treatment excessive or still too subtle;
- GH-132 closes or changes materially;
- another active capsule acquires overlapping ownership.

## Decisions and assumptions


- P3's semantic-series treatment is accepted and inherited unchanged.
- P3R1 corrects selected-date affordance only.
- Selected-bar recoloring is explicitly approved by the human owner.
- `selectionColor` remains the existing theme text/selection color already
  supplied by `HistoryScreen`; no new theme token is justified.
- A selected numeric/zero bar retains visible category identity through its
  original `barColor` outline.
- The selected numeric/zero outline is exactly 2 pixels.
- Selected gap/unavailable dates use a 16-by-3-pixel rounded baseline dash.
- The physically undersized circular marker is removed.
- Focused arbitrary-nutrient History remains generic accent.
- No material assumption remains unresolved at READY.
- Revision 2 records execution start from exact READY checkpoint
  `31b9a7787ac852e7c384839e59dc524d8b4d980b`. P3R1 remains bounded to the shared History chart,
  the two HistoryScreen selection-color prop call sites, and the direct E4-10
  rendering test. The accepted P3 four-series mapping, focused arbitrary
  nutrient accent behavior, History models/data, theme values, chart geometry,
  scrolling, reference-line behavior, selected-day reporting, and
  accessibility-selected state remain frozen.

## State history


| Date | From | To | Actor | Reason/evidence |
| --- | --- | --- | --- | --- |
| 2026-08-22 | — | DRAFT | ChatGPT | P3 physical review accepted semantic-series differentiation but requested a stronger selected-date affordance. |
| 2026-08-22 | DRAFT | GRILLED | ChatGPT / human owner | Physical review identified the 3-pixel-radius circle as too subtle on phone scale and explicitly approved selected-bar recoloring. |
| 2026-08-22 | GRILLED | SPECIFIED | ChatGPT | Selected fill plus 2-pixel original-series outline and a 16-by-3-pixel rounded empty-slot dash were frozen. |
| 2026-08-22 | SPECIFIED | DECOMPOSED | ChatGPT | Correction bounded to shared History chart rendering, its two selection-color call sites, and direct E4-10 rendering tests. |
| 2026-08-22 | DECOMPOSED | READY | ChatGPT | Cancelled predecessor is archived; exact P3 production authorities are rebound; focused History/theme/navigation baseline and TypeScript pass; corrected selection contract is frozen. |
| 2026-08-22 | READY | IN_PROGRESS | ChatGPT | Strict execution validation passed on exact READY checkpoint `31b9a7787ac852e7c384839e59dc524d8b4d980b`. Execution begins within the frozen P3R1 selection-affordance boundary. |

## Completion record

- **Reviewed commit:** TBD
- **Merged commit:** TBD
- **Review disposition:** TBD
- **Verification summary:** IN_PROGRESS — execution began from the strictly qualified READY checkpoint; implementation and mechanical qualification are not yet complete.
- **Specialized qualification:** PENDING — native iPhone-sized light/dark review is required after implementation.
- **Known warnings:** No P3R1 defect is known at READY. P3's prior repository audit contained one unrelated nonblocking migration warning.
- **Deferred work/follow-up IDs:** Later GH-132 semantic-color slices after P3R1.
- **Retrospective required:** no — evaluate after the complete GH-132 semantic-color system.
