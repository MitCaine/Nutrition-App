# Active task capsules

Store capsules here from `DRAFT` through `REVIEWED`. A capsule remains active while blocked,
under bounded correction, or awaiting commit/merge. Do not store generated logs, review ZIPs,
credentials, dependency trees, or agent scratch files here.
