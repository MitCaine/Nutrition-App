+++
schema_version = 1
capsule_revision = 5
id = "GH-154"
title = "Remove credential diagnostics and retire stale WF-003 authority"
state = "READY"
task_type = "correction"
risk = "medium"
created = "2026-08-22"
updated = "2026-08-22"
source_issue = "https://github.com/MitCaine/Nutrition-App/issues/154"
base_commit = "79898b71f7319b7b7892ef0fbe59617d1966fad5"
branch = "gh-154-pre-2-0-hygiene"
controller = "ChatGPT"
executor = "ChatGPT"
reviewer = "ChatGPT"
delegation = "none"
delegation_constraints = []
blocked = false
blocked_reason = ""
blocked_since = ""
dependencies = [
  "GitHub issue #22 — completed",
  "GH-153-R2 — completed",
]
planning_artifacts = [
  "engineering/workflow/TASK_CAPSULE.md",
  "engineering/workflow/STATES.md",
  "scripts/validate-task-capsules.py",
  "engineering/capsules/active/WF-003-implementation-result-artifacts.md",
  "apps/mobile/src/runtime/local/usdaCredentialStore.ts",
]
owned_paths = [
  "engineering/capsules/active/GH-154.md",
  "engineering/capsules/completed/GH-154.md",
  "apps/mobile/src/runtime/local/usdaCredentialStore.ts",
  "engineering/capsules/active/WF-003-implementation-result-artifacts.md",
  "engineering/capsules/completed/WF-003-implementation-result-artifacts.md",
]
allowed_paths = [
  "apps/mobile/__tests__/usdaCredentialStore.test.ts",
]
forbidden_paths = [
  "apps/backend/**",
  "apps/mobile/src/runtime/remote/**",
  "apps/mobile/src/shared/api/**",
  "apps/mobile/package.json",
  "apps/mobile/package-lock.json",
  ".github/**",
  "docs/**",
  "engineering/workflow/**",
]
specialized_qualification = []
+++

# GH-154 — Remove credential diagnostics and retire stale WF-003 authority

## Goal

Resolve the two bounded pre-2.0 hygiene findings in GitHub issue #154 without
changing USDA credential semantics or silently reviving historical Workflow v3
experimental work.

## Outcome

Ordinary USDA credential reads emit no credential value, configured-state, or
exact-length diagnostic, while credential lookup/storage behavior remains
unchanged. WF-003 is explicitly cancelled and archived as historical workflow
evidence rather than remaining as stale active authority.

## Non-goals

- Do not redesign USDA credential storage or SecureStore ownership.
- Do not change local/remote runtime authority.
- Do not introduce replacement credential diagnostics.
- Do not implement or re-anchor the WF-003 implementation-result renderer.
- Do not delete WF-003 history or historical handoff evidence.
- Do not refactor unrelated application, workflow, dependency, CI, or release
  surfaces.

## Background

The pre-2.0 repository audit found one unnecessary credential metadata log and
one stale active workflow capsule.

`usdaCredentialStore.ts` trims a stored credential and currently logs whether a
credential is configured plus the exact trimmed length. Neither datum is needed
for runtime behavior.

WF-003 was returned from READY to DECOMPOSED on 2026-08-06 and blocked pending
E1-18 / GitHub issue #22. Issue #22 is now complete. Its prior handoff is
explicitly historical and cannot be executed without re-anchoring.

For the 2.0 boundary, no current release requirement justifies introducing the
medium-risk implementation-result renderer experiment. Leaving WF-003 active
would preserve stale workflow authority, while re-anchoring it would create new
non-release-critical tooling scope. The bounded disposition is therefore to
cancel and archive WF-003 while preserving its history.

## Authority and precedence

1. GitHub issue #154.
2. `engineering/workflow/TASK_CAPSULE.md`.
3. `engineering/workflow/STATES.md`.
4. `scripts/validate-task-capsules.py`.
5. Existing USDA credential-store behavior.
6. Existing WF-003 capsule history.
7. This capsule.

Higher-authority conflicts stop execution.

## Dependencies and prerequisites

- GitHub issue #153 is complete before this task branch is created.
- GitHub issue #22 is complete.
- Default-branch authority includes the completed GH-153-R2 archive.
- Exact credential source, direct-test seam, and bootstrap topology are bound by the SPECIFIED contract.
- WF-003 must remain historical; no old handoff may be executed.
- Execution order is `GH-154-C -> GH-154-W -> GH-154-Q`.
- No implementation package may run before a later READY capsule-only overlay passes strict execution preflight.

## Owned surface

- GH-154 task-capsule lifecycle.
- `apps/mobile/src/runtime/local/usdaCredentialStore.ts`.
- The active-to-completed lifecycle disposition of WF-003.

### DECOMPOSED package ownership

Execution order is `GH-154-C -> GH-154-W -> GH-154-Q`.

`GH-154-C` owns only the credential source and its direct credential-store
test. It removes the unconditional diagnostic, preserves all credential
semantics, adds explicit no-log evidence, and treats the bootstrap test as a
byte-identical qualification authority.

`GH-154-W` owns only the two WF-003 path names required for its active-to-
completed move. It performs the exact revision-3 `DECOMPOSED` to revision-4
`CANCELLED` terminal transition, clears stale blocker metadata, preserves the
historical handoff, and records the cancellation rationale.

`GH-154-Q` owns no repository mutation. It qualifies the cumulative four-path
implementation under Node 24 and the repository workflow/audit gates.

Package checkpoints produce external evidence only. No intermediate
implementation commit is authorized. The cumulative candidate becomes eligible
for `IMPLEMENTED` only after `GH-154-Q` passes.

## Allowed changes

- Focused mobile tests may be added or adjusted only where necessary to prove
  unchanged credential semantics and absence of the production diagnostic.
- WF-003 may receive only the metadata, state-history, completion-record, and
  archive-location changes required for a truthful `DECOMPOSED -> CANCELLED`
  transition.

## Forbidden changes

- No backend, migration, database, API, package, lockfile, CI, or deployment
  changes.
- No remote-runtime or generic API-client change.
- No change to credential key names, normalization, SecureStore access,
  configured-state semantics, empty-key rejection, storage, or removal
  behavior.
- No development replacement diagnostic unless planning is revised first.
- No WF-003 renderer implementation, re-anchoring, READY transition, or
  execution.
- No deletion or rewriting of historical WF-003 evidence.

## Acceptance criteria

- [ ] AC-1: Ordinary execution of `getStoredUsdaCredential` emits no credential value, configured-state, or exact-length diagnostic.
- [ ] AC-2: Credential reads preserve trimming and return the trimmed credential or `null` exactly as before.
- [ ] AC-3: `isUsdaCredentialConfigured`, `setStoredUsdaCredential`, and `removeStoredUsdaCredential` preserve their existing behavior exactly.
- [ ] AC-4: Focused credential-store/runtime tests prove the preserved read, configured-state, write, empty-value rejection, and removal semantics and the absence of the removed diagnostic.
- [ ] AC-5: WF-003 transitions exactly from revision 3 `DECOMPOSED` to revision 4 `CANCELLED`, moves from `engineering/capsules/active` to `engineering/capsules/completed`, and remains historically truthful.
- [ ] AC-6: WF-003 cancellation records that issue #22 completed but the experimental renderer is not justified for the pre-2.0 boundary; the historical handoff is not resumed or deleted.
- [ ] AC-7: No application/runtime surface outside the credential store and its focused tests changes.
- [ ] AC-8: No workflow surface outside GH-154 and WF-003 changes.
- [ ] AC-9: Task-capsule validation, whitespace validation, affected mobile qualification, and repository audit pass.
- [ ] AC-10: Issue #154 remains open through implementation, verification, review, integration, and archive qualification.

## Required verification

### Focused

- Under repository Node 24 authority, run exactly
  `__tests__/usdaCredentialStore.test.ts` and
  `__tests__/localUsdaCredentialBootstrap.test.ts`.
- Prove the direct credential-store suite preserves normalized reads,
  configured/unconfigured state, normalized writes, blank-value rejection,
  removal, and explicit no-`console.log` behavior during ordinary reads.
- Prove `localUsdaCredentialBootstrap.test.ts` remains unchanged and still
  establishes request-time provider wiring without eager credential access.
- Prove no production credential diagnostic value, configured-state, or exact
  normalized-length output remains.
- Prove WF-003 is revision 4 `CANCELLED`, is present only under `completed`,
  has cleared blocker metadata, and preserves its historical handoff record.

### Baseline

- Run mobile TypeScript qualification under repository Node 24 authority.
- Run the full ordinary mobile Jest baseline with `--runInBand`.
- Run repository task-capsule validation.
- Run `git diff --check`.
- Run `apps/backend/.venv/bin/python scripts/project-audit.py pre-commit`; if it creates or modifies an additional repository path, stop rather than absorbing that drift into GH-154.

### Specialized qualification

Not applicable — this bounded hygiene correction changes no database,
migration, remote API, performance, native build, accessibility, or deployment
contract.

## Return evidence

- Exact source and task commit identities.
- Credential-store changed lines and focused-test inventory.
- Credential semantic-preservation evidence.
- Proof that no value, presence, or exact-length diagnostic remains.
- WF-003 pre-cancellation revision/state and final archived revision/state.
- Exact changed-path inventory.
- Exact validation, audit, and test commands with outcomes.
- Warnings, deviations, deferred work, and reviewer questions.
- Final evidence root and checksums.

## Escalation conditions

- Credential diagnostic removal requires changing storage or normalization
  semantics.
- No focused test seam exists and proving behavior would require unrelated
  runtime refactoring.
- WF-003 cannot legally transition `DECOMPOSED -> CANCELLED`.
- WF-003 cancellation requires rewriting or deleting historical evidence.
- Repository audit requires unrelated repair.
- Any backend, migration, API, package, lockfile, CI, or deployment change is
  required.
- Scope expands beyond issue #154.

## Decisions and assumptions

### READY execution authority

- Revision 5 promotes the stable revision-4 planning contract to `READY`.
- Exact execution `base_commit` is `79898b71f7319b7b7892ef0fbe59617d1966fad5`. This is the approved
  DECOMPOSED planning authority and must be the direct base used by strict
  execution validation.
- Decomposition-plan SHA-256 remains `7e6360bc5a1c2512f4b454feba0ba8746d5702ab92a4198f9abe1ad4bde61cf9`.
- Five-name lifecycle-plus-implementation execution-path SHA-256 remains
  `b1d30377733223477c47a9b2bbfccb4c3699d73a51dc1f631de91e2222c2eb94`.
- Four-name non-GH-154 implementation-path SHA-256 remains
  `81c77e96bcc1614d98be7811fc66b7accf45f738de071a53666b2ef82b0b156c`.
- Project-audit CLI evidence SHA-256 remains `f7d4bc1918d12156464f974f7ff707f4f3abba98bd30cffd238b346e72bc4e47`.
- Execution order remains exactly `GH-154-C -> GH-154-W -> GH-154-Q`.
- Credential source/test authority, protected bootstrap authority, WF-003
  source authority, acceptance criteria, risk, delegation, qualification,
  escalation conditions, and scope are unchanged from revision 4.
- This READY transition authorizes no implementation by itself until the
  committed capsule-only overlay passes strict `--execution` validation.
- After strict execution validation passes, the next legal lifecycle action is
  `READY -> IN_PROGRESS`; only then may package GH-154-C mutate repository
  implementation paths.
- Issue #154 remains open and default branch remains unchanged.

### DECOMPOSED execution plan

- Decomposition evidence SHA-256 is `7e6360bc5a1c2512f4b454feba0ba8746d5702ab92a4198f9abe1ad4bde61cf9`.
- Exact lifecycle-plus-implementation execution-path SHA-256 is
  `b1d30377733223477c47a9b2bbfccb4c3699d73a51dc1f631de91e2222c2eb94` for five path names.
- Exact non-GH-154 implementation-path SHA-256 is
  `81c77e96bcc1614d98be7811fc66b7accf45f738de071a53666b2ef82b0b156c` for four path names.
- Execution is split into exactly three ordered packages:
  `GH-154-C -> GH-154-W -> GH-154-Q`.
- `GH-154-C` starts from credential source blob `53d273f4f4a2378ef1460090d5fa293d97b6088c` and direct
  test blob `0c78f5b62013180bdd6f864e599b6a5f6ce556ad`. It may change only those two paths.
  Production removes only the unconditional diagnostic block; the direct test
  adds explicit no-`console.log` evidence while preserving existing semantic
  assertions.
- The bootstrap topology test remains protected at blob
  `73fe43318699f15e7d15f308bb6d94bef731f80d` and is qualification-only.
- `GH-154-W` starts from WF-003 blob `b5213368a45b08264c15d3bd4bd3c55f6d26596c` and may change only its
  active and completed path names. It performs revision 3 `DECOMPOSED` to
  revision 4 `CANCELLED`, clears stale blocker metadata, records a meaningful
  cancellation completion disposition, and preserves the historical handoff.
- `GH-154-Q` performs no repository mutation. It proves the exact cumulative
  four-path implementation and runs focused Jest, mobile typecheck, full mobile
  Jest, task-capsule validation, `project-audit.py pre-commit`, and whitespace
  validation under Node 24.
- Project-audit CLI evidence SHA-256 is `f7d4bc1918d12156464f974f7ff707f4f3abba98bd30cffd238b346e72bc4e47`. The repository audit
  requires the `pre-commit` subcommand; the earlier shorthand command without a
  subcommand is superseded by this executable CLI contract.
- If project audit produces an additional repository mutation, execution stops
  rather than broadening GH-154.
- Package checkpoints are external evidence only. No intermediate product,
  workflow, or lifecycle commit occurs.
- No implementation edit is authorized until a later revision-5 READY
  capsule-only overlay passes strict execution validation.

### SPECIFIED outcome contract

- Specification evidence SHA-256 is `fb999223c989a81131952e9814a7f657eb58c18642e86a2d6bcbf6dc757554a0`.
- Production credential authority remains blob `53d273f4f4a2378ef1460090d5fa293d97b6088c`. The only
  production behavior change is removal of the existing unconditional
  credential diagnostic `console.log` block; no replacement diagnostic is
  authorized.
- Credential normalization, null semantics, configured-state calculation,
  storage normalization, blank-value rejection, SecureStore key ownership, and
  credential removal semantics are fixed as preserved behavior.
- The only authorized test mutation is
  `apps/mobile/__tests__/usdaCredentialStore.test.ts`, initially blob
  `0c78f5b62013180bdd6f864e599b6a5f6ce556ad`. Existing semantic assertions remain and focused
  evidence must explicitly prove ordinary configured and blank/absent reads do
  not call `console.log`.
- `apps/mobile/__tests__/localUsdaCredentialBootstrap.test.ts`, blob
  `73fe43318699f15e7d15f308bb6d94bef731f80d`, is qualification-only and must remain byte-identical.
- WF-003 source authority remains blob `b5213368a45b08264c15d3bd4bd3c55f6d26596c` at revision 3
  `DECOMPOSED`. Execution must create revision 4 `CANCELLED`, clear stale
  blocker metadata, add exactly one truthful `DECOMPOSED -> CANCELLED`
  transition, and move the capsule from active to completed.
- WF-003 cancellation must state that issue #22 completed but the experimental
  renderer is not required for pre-2.0; its historical handoff remains
  historical and is neither resumed nor deleted.
- The exact implementation repository-path boundary is five names:
  GH-154 active capsule, USDA credential store, direct credential-store test,
  WF-003 active source, and WF-003 completed destination.
- No backend, remote-runtime, bootstrap-test, package, lockfile, CI, docs,
  workflow-rule, migration, database, or unrelated application change is
  authorized.
- Required qualification is Node-24 focused two-suite Jest, mobile typecheck,
  full ordinary mobile Jest, task-capsule validation, repository audit, and
  whitespace validation.
- Issue #154 remains open through implementation, verification, review,
  integration, MERGED archive, and post-push archive qualification.
- The observable outcome and acceptance interpretation are stable. No material
  requirement ambiguity remains.

### GRILLED ambiguity resolution

- Credential production authority is bound to `usdaCredentialStore.ts` blob
  `53d273f4f4a2378ef1460090d5fa293d97b6088c`. The intended production edit is removal of its single
  unconditional `console.log` diagnostic block only.
- Direct semantic coverage is bound to `usdaCredentialStore.test.ts` blob
  `0c78f5b62013180bdd6f864e599b6a5f6ce556ad`. Its four existing tests already cover normalized
  reads, null/configured state, normalized writes and blank rejection, and
  credential removal. This is the only authorized test mutation surface.
- `localUsdaCredentialBootstrap.test.ts` blob `73fe43318699f15e7d15f308bb6d94bef731f80d` proves the
  request-time credential-provider wiring and is qualification-only; no edit is
  expected or authorized there.
- Credential test-seam evidence SHA-256 is `bb6a471b7b102c2ffcf91144609f33044addad635d32c3e68daec72009025759`.
- WF-003 authority is bound to revision-3 `DECOMPOSED` blob `b5213368a45b08264c15d3bd4bd3c55f6d26596c`.
  Its original blocker, E1-18 / issue #22, is complete, but its prior handoff is
  explicitly historical and cannot be resumed.
- WF-003 will therefore transition exactly `DECOMPOSED -> CANCELLED`, revision
  3 to 4, and move from active to completed during GH-154 execution. No
  re-anchoring or renderer implementation is justified for the pre-2.0
  boundary.
- WF-003 disposition evidence SHA-256 is `2a789f0f1aa26f66b27028c2be9ad590b1154ecf0a657874af94c752525323a8`.
- Qualification is fixed to focused credential-store/bootstrap Jest, mobile
  typecheck, full ordinary mobile Jest, task-capsule validation, repository
  audit, and whitespace validation under repository Node 24 authority.
- Qualification-contract SHA-256 is `d9aac76866f50a013513406ca7916fb8c1744af6c80aac5d5c6ae38b2c9ddcb7`.
- No material ambiguity remains about the credential change, test seam,
  WF-003 disposition, authority boundary, or required qualification.

- Remove the USDA credential diagnostic entirely rather than replacing it with
  a development-only diagnostic; no runtime requirement currently needs it.
- Cancel/archive WF-003 rather than re-anchor it. The implementation-result
  renderer remains a historical experiment, not a pre-2.0 prerequisite.
- Preserve both WF-003 history and any historical handoff/evidence.
- Focused test ambiguity is resolved: `usdaCredentialStore.test.ts` is the
  only authorized test mutation surface; `localUsdaCredentialBootstrap.test.ts`
  is an unchanged qualification-only topology check.
- No unresolved architectural decision is introduced by these two bounded
  corrections.

## State history

| Date | From | To | Actor | Reason/evidence |
| --- | --- | --- | --- | --- |
| 2026-08-22 | — | DRAFT | ChatGPT | Created from GitHub issue #154 after GH-153 completion; bounded the task to credential-diagnostic removal and explicit WF-003 cancellation/archive without semantic or workflow expansion. |
| 2026-08-22 | DRAFT | GRILLED | ChatGPT | Resolved the exact credential source/test seam and WF-003 disposition: remove only the unconditional credential diagnostic, extend only the direct credential-store test if needed, retain bootstrap topology as an unchanged qualification target, and cancel/archive rather than re-anchor WF-003. Qualification commands and evidence hashes were fixed. |
| 2026-08-22 | GRILLED | SPECIFIED | ChatGPT | Froze the observable credential-preservation/no-diagnostic contract, exact direct-test and qualification-only bootstrap boundaries, exact WF-003 revision-4 CANCELLED archive outcome, five-path execution scope, and mandatory Node-24 qualification. |
| 2026-08-22 | SPECIFIED | DECOMPOSED | ChatGPT | Divided execution into credential correction, WF-003 cancellation/archive, and no-mutation cumulative qualification packages; froze exact five-name lifecycle/execution and four-name implementation path sets, corrected the repository audit invocation to its required pre-commit subcommand, and forbade intermediate implementation commits. |
| 2026-08-22 | DECOMPOSED | READY | ChatGPT | Bound execution base to `79898b71f7319b7b7892ef0fbe59617d1966fad5`, preserved the exact credential/WF-003 package contract and qualification authority, and authorized execution only after the committed capsule-only overlay passes strict execution validation. |

## Completion record

- **Reviewed commit:**
- **Merged commit:**
- **Review disposition:**
- **Verification summary:**
- **Specialized qualification:**
- **Known warnings:**
- **Deferred work/follow-up IDs:**
- **Retrospective required:** no — bounded pre-release hygiene correction.
