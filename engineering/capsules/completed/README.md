# Completed task capsules

Store capsules here after `MERGED`, `RETROSPECTED`, or `CANCELLED` completion records are
filled. Completed capsules are durable historical records. Do not repurpose an ID; corrections
are append-only and identify the correcting commit.
