+++
schema_version = 1
capsule_revision = 9
id = "GH-147"
title = "Refresh Expo SDK 57 compatibility patch versions"
state = "MERGED"
task_type = "correction"
risk = "medium"
created = "2026-08-20"
updated = "2026-08-20"
source_issue = "https://github.com/MitCaine/Nutrition-App/issues/147"
base_commit = "98db03fa838d425476ec6e5d41f90804f525808a"
branch = "gh-147-expo-sdk57-patches"
controller = "ChatGPT GPT-5.6 Sol controller"
executor = "ChatGPT GPT-5.6 Sol bounded executor"
reviewer = "Independent review pass"
delegation = "none"
delegation_constraints = []
blocked = false
blocked_reason = ""
blocked_since = ""
dependencies = []
planning_artifacts = [
  "engineering/workflow/TASK_CAPSULE.md",
  "engineering/workflow/STATES.md",
  "docs/operations/testing.md",
  ".github/workflows/ci.yml",
]
owned_paths = [
  "engineering/capsules/active/GH-147.md",
  "apps/mobile/package.json",
  "apps/mobile/package-lock.json",
]
allowed_paths = []
forbidden_paths = [
  ".github/workflows/ci.yml",
  "apps/mobile/src/",
  "apps/mobile/app.json",
  "apps/mobile/app.config.*",
  "apps/mobile/ios/",
  "apps/mobile/android/",
  "modules/",
  "apps/backend/",
  "packages/shared-contracts/",
  "docs/project/version-1.2/",
  "engineering/capsules/completed/",
]
specialized_qualification = [
  "Use repository Node 24 for dependency installation and qualification.",
  "Run npm exec -- expo install --check and require zero Expo compatibility drift.",
  "Run npx expo-doctor and require a clean project-health result.",
  "Run the full mobile TypeScript check and full Jest suite.",
  "After merge, require the default-branch Mobile baseline to return green before closing #147.",
]
+++

# GH-147 — Refresh Expo SDK 57 compatibility patch versions

## Goal

Restore the ordinary Mobile baseline after Expo SDK 57 advanced its expected compatible patch
versions upstream.

## Outcome

The five Expo-managed dependencies identified by `expo install --check` use the currently expected
SDK 57 patch ranges, the npm lockfile is regenerated normally, local mobile qualification passes,
and the default-branch Mobile baseline returns green.

## Non-goals

- No Expo SDK major or minor upgrade.
- No React or React Native upgrade.
- No unrelated dependency refresh.
- No application behavior change.
- No native-project regeneration.
- No source-code compatibility repair unless a patch demonstrably requires one and the controller
  explicitly widens scope.

## Background

GitHub #147 records the only persistent Mobile baseline failure after completed Epic 4 work.
`npm exec -- expo install --check` requires these patch updates:

- `expo` to `~57.0.15`
- `expo-camera` to `~57.0.4`
- `expo-file-system` to `~57.0.5`
- `expo-image-picker` to `~57.0.12`
- `expo-sharing` to `~57.0.14`

Backend baseline, PostgreSQL 16 contracts, and repository validation were already unrelated and
green when this maintenance issue was created.

## Authority and precedence

1. GitHub #147 owns the compatibility correction.
2. `npm exec -- expo install --check` defines the currently expected Expo SDK 57 package ranges.
3. `apps/mobile/package.json` and generated `package-lock.json` own the mobile dependency state.
4. `.github/workflows/ci.yml` owns the existing Mobile baseline and remains unchanged.

## Dependencies and prerequisites

- Exact base commit is `98db03fa838d425476ec6e5d41f90804f525808a`.
- Repository Node 24 is required.
- The current mobile baseline already runs Expo compatibility, Expo Doctor, TypeScript, and Jest.
- No product or architecture dependency is required.

## Owned surface

- `apps/mobile/package.json`
- `apps/mobile/package-lock.json`
- this task capsule

## Allowed changes

Not applicable — the required repair should remain entirely inside the exact owned paths.

## Forbidden changes

- No mobile application source changes.
- No native iOS or Android project changes.
- No Expo SDK major/minor migration.
- No React or React Native version change.
- No unrelated package upgrade.
- No CI workflow change.
- No backend, migration, shared-contract, current-guide, or completed-capsule change.

## Acceptance criteria

- [x] AC-1: `expo` is updated only to the Expo-required SDK 57 patch range `~57.0.15`.
- [x] AC-2: `expo-camera` is updated only to `~57.0.4`.
- [x] AC-3: `expo-file-system` is updated only to `~57.0.5`.
- [x] AC-4: `expo-image-picker` is updated only to `~57.0.12`.
- [x] AC-5: `expo-sharing` is updated only to `~57.0.14`.
- [x] AC-6: No other direct dependency range changes.
- [x] AC-7: `package-lock.json` is regenerated through normal npm/Expo tooling.
- [x] AC-8: `npm exec -- expo install --check` passes.
- [x] AC-9: `npx expo-doctor` passes.
- [x] AC-10: Mobile TypeScript passes.
- [x] AC-11: Full mobile Jest passes.
- [x] AC-12: No source, native-project, CI, backend, migration, or unrelated dependency change.
- [x] AC-13: The existing Mobile baseline command set passes locally under repository Node 24 before review; default-branch CI remains a post-merge issue-closure gate.

## Required verification

### Focused

- Compare direct dependency ranges before and after the update.
- Prove only the five issue-authorized Expo dependencies changed in `package.json`.
- Inspect the generated lockfile delta for only consequences of those five patch updates.
- Run `npm exec -- expo install --check`.
- Run `npx expo-doctor`.

### Baseline

- Run `npm run typecheck`.
- Run full `npm test -- --runInBand`.
- Run both repository Expo configuration contracts if required by the existing Mobile baseline.
- Run task-capsule validation.
- Run `./scripts/session-end.sh`.
- Run `git diff --check`.

### Specialized qualification

- Use repository Node 24.
- Default-branch GitHub Actions Mobile baseline must pass after integration.
- No physical-device qualification is required unless the patch unexpectedly changes behavior.

## Return evidence

- Exact branch/base/origin proof.
- Exact Node and npm versions.
- Exact direct dependency before/after values.
- Exact changed paths.
- Lockfile-generation command.
- Expo compatibility result.
- Expo Doctor result.
- TypeScript result.
- Full Jest result/counts.
- Capsule/session-end/whitespace results.
- Any warning, unexpected transitive change, or attempted scope expansion.
- Final Git status.

## Escalation conditions

- Stop if Expo proposes any direct dependency beyond the five packages authorized by #147.
- Stop if React, React Native, Expo SDK major/minor, or another direct dependency would change.
- Stop if any application/native source change is required.
- Stop if Expo Doctor exposes an unrelated project-health defect that cannot be resolved within the
  exact dependency patch.
- Stop if full Jest or TypeScript exposes a product regression rather than patch-only compatibility.

## Decisions and assumptions

- Revision 9 corrects completion-record metadata after archive validation rejected the blank `Deferred work/follow-up IDs` field. No work is deferred from GH-147; existing npm audit, transitive deprecation, and `fsevents` allow-scripts warnings remain explicitly outside this task. Product scope, review disposition, merged commit, CI evidence, and MERGED state are unchanged.
- Revision 8 records human-owned squash integration at `34537db78b53a5a2766e0d1e261443baa3327c77` with exact reviewed tree `2783879107a2724cd19901bac6e0bb4079b582af`, followed by the required default-branch CI closure gate. GitHub Actions run `32408980955` (https://github.com/MitCaine/Nutrition-App/actions/runs/32408980955) completed with Mobile baseline `success` (workflow conclusion `success`). GH-147 is therefore MERGED and eligible for archival and issue closure. The previously recorded npm audit, `fsevents` allow-scripts, and transitive deprecation warnings remain outside this bounded compatibility correction.
- Revision 7 records independent review approval of exact implementation commit `6eedd50df089a7d8e3340d206fc854c4f5e5dd6d`, tree `f1213b5d16b79495b8842a6064ff6fa6d96873f5`. Review confirmed the direct manifest changes are limited to the five #147-authorized Expo SDK 57 patch ranges, React and React Native remain unchanged, the generated lockfile delta is attributable to the authorized Expo patch refresh, and no application source, native project, CI, backend, migration, or unrelated dependency surface changed. Exact-commit Node 24 qualification passed clean install, Expo configuration, dependency compatibility, Expo Doctor, TypeScript, full Jest, repository audit, and scope checks. Recorded npm audit/deprecation/allow-scripts warnings do not justify widening this maintenance task. Review disposition is Approved — PASS. GH-147 advances to REVIEWED pending human-owned integration; default-branch Mobile CI remains the post-merge closure gate.
- Revision 6 records exact-commit verification of implementation commit `6eedd50df089a7d8e3340d206fc854c4f5e5dd6d`, tree `f1213b5d16b79495b8842a6064ff6fa6d96873f5`. The committed manifest changes only the five #147-authorized Expo SDK 57 direct dependency ranges; React and React Native are unchanged. A clean `npm ci` under Homebrew Node 24.19.0 reproduced the lockfile, both Expo configuration contracts passed, `expo install --check` reported dependencies current, Expo Doctor passed, TypeScript passed, and full mobile Jest passed. The existing npm audit, `fsevents` allow-scripts, and transitive deprecation warnings remain recorded and did not authorize scope expansion. GH-147 advances to VERIFIED pending independent review.
- Revision 5 records the npm install warnings observed during the authorized patch refresh. They are retained as evidence rather than treated as permission to run `npm audit fix` or widen dependency scope. The IMPLEMENTED state and three-path implementation candidate are otherwise unchanged.
- Revision 4 records bounded implementation under v24.19.0 / npm 11.17.0 from `/opt/homebrew/opt/node@24`. Only the five #147-authorized Expo SDK 57 direct dependency ranges changed: `expo ~57.0.15`, `expo-camera ~57.0.4`, `expo-file-system ~57.0.5`, `expo-image-picker ~57.0.12`, and `expo-sharing ~57.0.14`. Normal Expo/npm tooling regenerated the lockfile. Both Expo configuration contracts, `expo install --check`, Expo Doctor, TypeScript, full mobile Jest, resolved-version proof, exact scope proof, and whitespace checks passed. No application source, native project, React/React Native, CI, backend, migration, or unrelated direct dependency changed. Default-branch Mobile CI remains the post-merge #147 closure gate.
- Revision 2 corrects one controller workflow mistake in the READY capsule: default-branch CI occurs only after human integration, so it cannot be a pre-merge acceptance checkbox required for VERIFIED/REVIEWED. AC-13 now owns the exact CI-equivalent local Mobile baseline under Node 24. The actual default-branch Mobile baseline remains mandatory after merge before #147 closes. Package scope, issue scope, risk, and qualification are unchanged.
- This is patch-level SDK maintenance, not an Expo migration.
- The five package ranges reported by the failing compatibility gate are the complete authorized
  direct-dependency change set.
- Existing CI structure is correct and does not need modification.
- #147 remains open until the post-merge default-branch Mobile baseline is green.

## State history

| Date | From | To | Actor | Reason/evidence |
| --- | --- | --- | --- | --- |
| 2026-08-20 | — | DRAFT | ChatGPT GPT-5.6 Sol controller | #147 records the isolated Expo SDK 57 patch drift. |
| 2026-08-20 | DRAFT | GRILLED | ChatGPT GPT-5.6 Sol controller | Drift is bounded to five explicitly reported Expo-managed patch dependencies. |
| 2026-08-20 | GRILLED | SPECIFIED | ChatGPT GPT-5.6 Sol controller | Exact package ranges and qualification gates are defined by #147 and existing Mobile CI. |
| 2026-08-20 | SPECIFIED | DECOMPOSED | ChatGPT GPT-5.6 Sol controller | Work is bounded to package manifest, generated lockfile, and capsule. |
| 2026-08-20 | DECOMPOSED | READY | ChatGPT GPT-5.6 Sol controller | Exact base, scope, Node requirement, tests, and escalation conditions are explicit. |
| 2026-08-20 | READY | DECOMPOSED | ChatGPT GPT-5.6 Sol controller | Corrected the impossible pre-merge/default-branch CI acceptance ordering; implementation scope is unchanged. |
| 2026-08-20 | DECOMPOSED | READY | ChatGPT GPT-5.6 Sol controller | Pre-merge Mobile baseline qualification and post-merge #147 closure gate are now separated explicitly. |

| 2026-08-20 | READY | IN_PROGRESS | ChatGPT GPT-5.6 Sol bounded executor | Strict execution preflight passed at exact execution base `25342fd512522d1209cc62d4c1ad409187ed0084`; bounded Expo SDK 57 patch maintenance begins under Homebrew Node 24.19.0. |

| 2026-08-20 | IN_PROGRESS | IMPLEMENTED | ChatGPT GPT-5.6 Sol bounded executor | Five authorized Expo SDK 57 patches and generated lockfile updated; CI-equivalent Mobile qualification passed under Node 24.19.0. |
| 2026-08-20 | IMPLEMENTED | VERIFIED | ChatGPT GPT-5.6 Sol controller | Exact implementation commit `6eedd50df089a7d8e3340d206fc854c4f5e5dd6d` and tree `f1213b5d16b79495b8842a6064ff6fa6d96873f5` passed clean-install, Expo compatibility/health/configuration, TypeScript, full Jest, scope, resolved-version, and whitespace verification under Node 24.19.0. |
| 2026-08-20 | VERIFIED | REVIEWED | ChatGPT GPT-5.6 Sol independent reviewer | PASS — exact implementation commit `6eedd50df089a7d8e3340d206fc854c4f5e5dd6d`, tree `f1213b5d16b79495b8842a6064ff6fa6d96873f5`, is a bounded five-package Expo SDK 57 patch refresh with generated lockfile consequences only; verification evidence is sufficient and no blocking finding remains. |
| 2026-08-20 | REVIEWED | MERGED | Human owner | Squash-integrated reviewed tree at `34537db78b53a5a2766e0d1e261443baa3327c77`; exact default-branch GitHub Actions run `32408980955` completed with Mobile baseline PASS. |


## Completion record

- **Reviewed commit:** 6eedd50df089a7d8e3340d206fc854c4f5e5dd6d
- **Merged commit:** 34537db78b53a5a2766e0d1e261443baa3327c77
- **Review disposition:** Approved — PASS. The exact implementation satisfies AC-1 through AC-13 with no unauthorized direct dependency, runtime, native-project, CI, or application-source change. Default-branch Mobile CI remains required after merge before #147 closes.
- **Verification summary:** PASS — exact implementation commit `6eedd50df089a7d8e3340d206fc854c4f5e5dd6d`, tree `f1213b5d16b79495b8842a6064ff6fa6d96873f5`, passed clean `npm ci`, both Expo configuration contracts, `expo install --check`, Expo Doctor, TypeScript, full mobile Jest, direct-manifest and resolved-version proof, exact three-path scope enforcement, and `git diff --check` under Node 24.19.0.
- **Specialized qualification:** PASS — local CI-equivalent Mobile baseline passed under repository Node 24.19.0 and default-branch GitHub Actions run `32408980955` completed with `Mobile baseline` = `success`.
- **Known warnings:** Expo/npm installation reported 16 dependency-audit findings (8 moderate, 8 high), the existing npm `allow-scripts` warning for optional `fsevents@2.3.3`, and deprecation notices from transitive packages including `inflight`, `glob@7`, and `uuid@7`. None required or authorized a #147 scope expansion; no `npm audit fix`, forced upgrade, script-approval mutation, React/React Native migration, or unrelated dependency update was performed.
- **Deferred work/follow-up IDs:** None — no deferred GH-147 work; the recorded npm audit, transitive deprecation, and `fsevents` allow-scripts warnings remain outside this bounded compatibility task.
- **Retrospective required:** no — routine bounded dependency patch unless qualification reveals a workflow defect.
